让我仔细审查整个交互流程的代码。
用户说还是不行，让我把所有逻辑理清。我需要仔细审查整个交互流程，找出问题所在。

让我重新理解问题：
1. 手持活末影珍珠右键扩展地图区域的空活地图时，触发的是物品交换逻辑，而非传送逻辑
2. 这意味着 `findGroupBySlot` 没有正确匹配到地图组

让我仔细看一下代码流程：

1. `LivingMapLayout.scan()` 遍历 `menu.slots`，用循环变量 `i` 作为 `slotIndex` 存储
2. `findGroupBySlot()` 用 `containsSlot(slotIndex)` 检查
3. 在 Mixin 中，用 `menu.slots.indexOf(this.hoveredSlot)` 获取列表索引

问题可能在于：
- `menu.slots.indexOf()` 可能返回 -1（如果 hoveredSlot 不在列表中）
- 或者 `Slot.index` 和列表位置的关系

让我重新审视 `Slot.index` 的含义。在 Minecraft 中，`Slot.index` 是该槽位在容器中的索引，而 `menu.slots` 是所有槽位的列表。对于大多数容器，`menu.slots.get(i).index` 可能不等于 `i`。

等等，实际上在 Minecraft 中，`AbstractContainerMenu.slots` 是一个 `List<Slot>`，而 `Slot.index` 是该槽位在容器中的索引。通常情况下，`menu.slots.get(i).index` 确实等于 `i`，因为槽位是按顺序添加的。但这不是保证的。

不过，更重要的是，让我看看 `hoveredSlot` 是什么。在 `AbstractContainerScreen` 中，`hoveredSlot` 是 `Slot` 类型，它是 `menu.slots` 中的一个元素。

所以 `menu.slots.indexOf(this.hoveredSlot)` 应该能正确返回列表位置。

让我再仔细看看代码，可能问题出在其他地方。

让我重新读一下完整的 mouseClicked 注入代码。

现在让我看看 `GuiInteractionHelper.tryInteract` 做了什么：

现在我完全理解了整个交互流程。让我理清问题：

---

## 完整逻辑梳理

### 交互流程（mouseClicked HEAD 注入）

```
1. 更新地图组缓存 (living_item$updateMapGroups)
2. 检查：右键 + hoveredSlot != null + 手持活末影珍珠
   → 用 menu.slots.indexOf(hoveredSlot) 查找地图组
   → 如果找到，发送 LivingMapGuiTeleportPacket，拦截（return true）
3. 调用 GuiInteractionHelper.tryInteract(hoveredSlot, button, menu)
   → 查询 InteractionRegistry：光标物品(trigger) + 槽位物品(target) + 按键
   → 活末影珍珠 + 空活地图 + 右键 → 匹配到交互规则 → 发送 GuiInteractionPacket
   → 返回 true，拦截
4. 其他逻辑...
```

### 问题根因

**步骤2和步骤3是串行的**。步骤2只在 `hoveredSlot` 属于地图组时拦截。但问题是：

- `menu.slots.indexOf(hoveredSlot)` 返回的索引是否与 `scan()` 中使用的 `i` 一致？**是的，应该一致**——`scan()` 遍历 `menu.slots`，用循环变量 `i` 作为 `slotIndex`，`indexOf` 也返回列表位置。

- 但等等——`indexOf` 用的是 `equals` 比较。`Slot` 类是否正确实现了 `equals`？如果没有，`indexOf` 会用引用比较，而 `hoveredSlot` 是 `menu.slots` 中的同一个引用，所以应该能匹配。

让我再想想... 问题可能是 **`hoveredSlot` 在扩展地图区域上方时为 null**！因为扩展地图纹理渲染在 z=300，覆盖了槽位，导致鼠标悬浮检测不到底层槽位。

不对，`hoveredSlot` 是在 `render` 中由原版代码计算的，不受我们的叠加层影响。它基于鼠标坐标和槽位位置计算。

让我再想想... 也许问题更简单：**`hoveredSlot` 可能确实不为 null，但 `indexOf` 返回 -1**？不太可能。

或者... 问题可能是 `living_item$mapGroups` 为空！让我检查 `updateMapGroups` 的逻辑：

我发现了！`computeContentHash` 用的是 `slot.index`，但 `scan()` 用的是列表位置 `i`。不过这只是哈希计算，不影响匹配。

让我再仔细想想真正的问题... 

等等！我需要确认一个关键问题：**`hoveredSlot` 在扩展地图覆盖区域上方时是否为 null？**

扩展地图纹理渲染在 z=300，但 `hoveredSlot` 的计算是基于 `AbstractContainerScreen` 的 `slotClicked`/`findSlot` 方法，它们检查的是鼠标坐标与槽位屏幕坐标的关系，不受 z 层影响。所以 `hoveredSlot` 应该能正常检测到。

那问题到底在哪？让我添加一些调试输出来确认...

实际上，让我重新审视：用户说"手持活末影珍珠右键属于扩展地图部分的空活地图时依旧触发物品交换逻辑"。

这意味着：
1. `hoveredSlot` 不为 null ✓
2. `living_item$isHoldingLivingEnderPearl()` 返回 true ✓
3. `findGroupBySlot` 返回 null ✗ ← 问题在这
4. 所以走到 `GuiInteractionHelper.tryInteract`，匹配到活末影珍珠+空活地图的交互规则

为什么 `findGroupBySlot` 返回 null？可能的原因：
- `menu.slots.indexOf(hoveredSlot)` 返回 -1
- 或者 `living_item$mapGroups` 为空
- 或者 `slotIndices` 中的值与 `indexOf` 结果不匹配

让我验证一下 `Slot.index` 和列表位置的关系：

现在让我看看 `AbstractContainerMenu` 如何设置 `Slot.index`：

关键发现！`slot.index = this.slots.size()` — `Slot.index` 就是 `menu.slots` 列表中的位置！所以 `slot.index` 和 `menu.slots.indexOf(slot)` 应该是一样的。

那问题不在这里。让我再想想...

等等，我需要确认 `hoveredSlot` 是否真的属于 `menu.slots`。让我看看 `hoveredSlot` 是怎么设置的：

好的，`hoveredSlot` 确实是 `menu.slots` 中的引用。`slot.index` 等于列表位置。

那问题到底在哪？让我再仔细看看 `scan()` 中的逻辑...

啊！我发现了！问题可能在于 **`hoveredSlot` 在扩展地图区域上方时可能为 null**！

看第 153 行：`if (this.isHovering(slot, (double)mouseX, (double)mouseY) && slot.isActive())`

`isHovering` 检查鼠标是否在槽位区域内。但扩展地图纹理渲染在 z=300，**覆盖了槽位**。不过 `isHovering` 是纯坐标计算，不受 z 层影响...

等等，让我重新看用户的描述："手持活末影珍珠右键属于扩展地图部分的空活地图时依旧触发物品交换逻辑"。

"物品交换逻辑" — 这意味着 `GuiInteractionHelper.tryInteract` 匹配到了交互规则。活末影珍珠 + 空活地图 + 右键 → 匹配到某个交互规则（可能是"交换位置"的规则）。

所以流程是：
1. 步骤2：`findGroupBySlot` 返回 null → 不拦截
2. 步骤3：`tryInteract` 匹配到交互规则 → 发送 GuiInteractionPacket → 返回 true → 拦截

**问题确认：`findGroupBySlot` 返回了 null。**

为什么？让我加个日志验证... 不，让我直接检查 `indexOf` 的行为。

实际上，我怀疑问题可能是 `menu.slots.indexOf(hoveredSlot)` 确实返回了正确的索引，但 `living_item$mapGroups` 为空列表！

为什么 `mapGroups` 可能为空？因为 `updateMapGroups` 只在哈希变化时更新。如果初始哈希为0，第一次调用时哈希计算结果也是0（如果没有活地图物品），那 `mapGroups` 就是空列表。但后来放了活地图物品后... 等等，哈希应该会变化。

让我再看看 `computeContentHash`：

```java
private int computeContentHash() {
    int h = 1;
    for (Slot slot : menu.slots) {
        ItemStack stack = slot.getItem();
        if (LivingItemManager.isLivingItem(stack) && (stack.is(Items.FILLED_MAP) || stack.is(Items.MAP))) {
            h = 31 * h + slot.index;
            h = 31 * h + slot.x;
            h = 31 * h + slot.y;
            h = 31 * h + stack.getItem().hashCode();
        }
    }
    return h;
}
```

这看起来没问题。如果容器中有活地图，哈希应该非零。

让我再想想... 也许问题是 `hoveredSlot.index` 和 `menu.slots.indexOf(hoveredSlot)` 确实相同，但 `scan()` 中存储的 `slotIndex` 有误？

不，`scan()` 中 `slotIndices[0] = topLeft.slotIndex`，而 `topLeft.slotIndex = i`（循环变量），`i` 就是 `menu.slots` 的列表位置。

我真的需要加日志来调试了。但在此之前，让我再检查一个可能性：

**`hoveredSlot` 可能指向的是 `SlotWrapper` 而不是原始 `Slot`！** 在创造模式中，槽位可能被包装。

让我检查：

`SlotWrapper` 只在创造模式中存在。用户应该是在普通容器（如箱子）中测试的。

让我换个思路——也许问题更根本：**`hoveredSlot` 在扩展地图覆盖区域上方时确实为 null**！

因为我们的扩展地图渲染在 z=300，虽然 `isHovering` 是坐标计算不受 z 影响，但... 等等，`hoveredSlot` 是在 `render` 方法中计算的，而我们的 `renderExpandedMaps` 注入在 `render` 的 TAIL。所以 `hoveredSlot` 在我们的代码运行时已经计算好了。

但是！**`mouseClicked` 注入在 HEAD**，此时 `hoveredSlot` 是上一帧渲染时计算的值。这应该没问题，因为 `hoveredSlot` 在每帧 `render` 时更新。

好吧，让我直接加日志来调试，这是最可靠的方式：

还需要确认 `LOGGER` 是否可用：

`LOGGER` 没有在这个类中定义。用 `LivingButton.LOGGER` 或者直接用 `System.out.println`：

同时在 `scan` 中也加日志：

**BUILD SUCCESSFUL** ✅

已添加调试日志。请进游戏测试，然后查看日志输出（在游戏控制台或 `run/logs/latest.log` 中搜索 `[LivingItem]`）。

需要关注的信息：
1. **`scan found group`** — 布局扫描是否找到了地图组，以及 `slotIndices` 的值
2. **`mouseClicked`** — 右键点击空活地图时，`hoveredSlot.index`、`slotListIdx`、`mapGroups.size`、`group` 的值

这样就能确定问题出在哪个环节了喵！