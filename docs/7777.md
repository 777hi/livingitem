The user is asking about popular mods that have done the work of becoming a library/API mod - specifically mods that have API package separation, custom DataComponent registration, event systems, Gradle dependency publishing, etc. Let me search for some well-known examples.

The search results aren't giving me great specific technical details. Let me rely on my knowledge of popular Minecraft mods that serve as API/library mods and have done the work of API separation, event systems, etc. I know these well:

1. **Curios API** - The most famous API mod for accessory slots
2. **Patchouli** - Data-driven book system
3. **Create** - Mechanical automation with addon system
4. **JEI (Just Enough Items)** - Recipe viewing API
5. **Architectury** - Multi-platform API layer
6. **GeckoLib** - Animation API
7. **Tinkers' Construct** - Tool building system with addon support

Let me provide a comprehensive answer based on my knowledge.
搜索结果不太理想，不过这些模组我比较熟悉，直接给你梳理一下喵~

## 热门 API/Lib 模组一览

### 1. Curios API（饰品栏）

| 做了什么 | 怎么做的 |
|----------|---------|
| **API 包分离** | ✅ `top.theillusivec4.curios.api/` 公开接口，`top.theillusivec4.curios.common/` 内部实现 |
| **注册式扩展** | ✅ 其他模组通过 `CuriosApi.registerCurio()` 注册自定义饰品类型 |
| **事件系统** | ✅ `CurioChangeEvent`、`CurioEquipEvent`，其他模组可以监听 |
| **Gradle 依赖** | ✅ 发布到 CurseForge Maven，其他模组直接 `implementation` |
| **Tag 匹配** | ✅ 用 `#curios:head`、`#curios:ring` 等 Tag 定义饰品槽位 |

**最像你的场景**：Curios 本身不提供任何饰品，纯粹是框架。其他模组（如 Botania、Ars Nouveau）声明 Curios 为前置，然后注册自己的饰品。

### 2. Patchouli（帕秋莉手册）

| 做了什么 | 怎么做的 |
|----------|---------|
| **API 包分离** | ✅ `vazkii.patchouli.api/` 公开，`vazkii.patchouli.common/` 内部 |
| **数据驱动** | ✅ 其他模组只需写 JSON 就能创建手册，不需要写一行 Java |
| **多模组手册** | ✅ Botania、Ars Nouveau、Blood Magic 等都用 Patchouli 做手册 |
| **Gradle 依赖** | ✅ 发布到 Maven |

**最值得借鉴**：数据驱动设计。其他模组不需要继承任何 Java 类，只要在资源包里放 JSON 文件就能接入。

### 3. Create（机械动力）

| 做了什么 | 怎么做的 |
|----------|---------|
| **API 包分离** | ✅ `com.simibubi.create.api/` 公开，`com.simibubi.create.content/` 内部 |
| **注册式扩展** | ✅ `CreateRegistries` 注册自定义机械组件 |
| **事件系统** | ✅ `CreateEvents` 提供旋转力、机械处理等事件 |
| **Addon 生态** | ✅ Create Deco、Create Crafts & Additions 等大量 Addon 模组 |
| **Gradle 依赖** | ✅ 发布到 Maven |

**最像你的场景**：Create 本身是功能模组，但通过 API 包分离让其他模组可以扩展。你的活物品模组也是"功能模组 + 框架"的双重身份。

### 4. JEI（Just Enough Items）

| 做了什么 | 怎么做的 |
|----------|---------|
| **API 包分离** | ✅ `mezz.jei.api/` 公开（接口、注册表），`mezz.jei.common/` 内部 |
| **注册式扩展** | ✅ `IRecipeRegistration` 注册自定义配方显示 |
| **插件系统** | ✅ 其他模组实现 `IModPlugin` 接口就能往 JEI 里添加内容 |
| **Gradle 依赖** | ✅ 发布到 CurseForge Maven |

### 5. Tinkers' Construct（匠魂）

| 做了什么 | 怎么做的 |
|----------|---------|
| **API 包分离** | ✅ `slimeknights.tconstruct.library/` 公开，`slimeknights.tconstruct.common/` 内部 |
| **注册式扩展** | ✅ 其他模组注册自定义材料、工具修饰符 |
| **事件系统** | ✅ 工具修改事件、工具破坏事件 |
| **Addon 生态** | ✅ 匠魂装甲、匠魂强化等大量 Addon |

---

## 对比你的模组

| 能力 | Curios | Patchouli | Create | JEI | **你的活物品** |
|------|--------|-----------|--------|-----|---------------|
| API 包分离 | ✅ | ✅ | ✅ | ✅ | ❌ 目前没有 |
| 注册式扩展 | ✅ | ✅ | ✅ | ✅ | ⚠️ 有 `registerFunction` 但不完整 |
| 事件系统 | ✅ | ❌ | ✅ | ❌ | ❌ 没有 |
| Tag 匹配 | ✅ | ❌ | ❌ | ❌ | ❌ 硬编码物品 |
| Gradle 发布 | ✅ | ✅ | ✅ | ✅ | ❌ 没有 |
| 数据驱动 | ❌ | ✅ | ❌ | ❌ | ❌ 没有 |

## 最值得参考的

你的模组和 **Create** 最像——本身是功能模组，但又有框架价值。Create 的做法是：

1. **`api/` 包放接口和注册表**，承诺稳定不随便改
2. **`content/` 包放具体实现**，内部细节随时可改
3. **注册式设计**：其他模组通过 `CreateRegistries` 注册扩展
4. **事件钩子**：关键节点抛事件，其他模组可以拦截

建议你先看看 Create 的 `api/` 包结构，它是最接近你需求的参考模板喵~