# 2026-09-22 工作日志

## 修复：活TNT 爆炸后光照不刷新（用户报告）

### 症状与根因

用户：「**活tnt爆炸后光照没有刷新喵。所有方块都黑黑的喵。**」

根因（读 `libs/src/neoforge-21.1.249-merged` 源码确认，**不是凭记忆**）：
- `NORMAL` 走 `level.setBlock()` → 最终进 `LevelChunk.setBlockState()`，
  **原版把光照更新写在那里**（`LevelChunk.java:258~270`）：
  * section 由非空变空 → `lightEngine.updateSectionStatus(pos, hasOnlyAir)`
  * `LightEngine.hasDifferentLightProperties(...)` 为真 → `lightEngine.checkBlock(pos)`
- `HIGH_YIELD` / `SUPER` 为性能**直接调 `LevelChunkSection.setBlockState()`** ⇒ **绕过**上面那层
  ⇒ 光照引擎完全不知道方块没了：`ChunkSkyLightSources`（天光柱高图）仍认为地下被堵死、
  光数据仍是炸之前的值 ⇒ `ClientboundLevelChunkWithLightPacket` 发出去的是**旧光照**。
- 代码里原本的注释其实已经写明「不触发任何回调/**光照更新**/客户端同步」——
  但没人把"光照更新"这半边补回来。

### 关键：先查源码再动手（本轮的主要方法论）

查清了三件事才写代码，避免又一次"凭推理下结论"：
1. **正确的重算入口**（`LevelLightEngine` 公开方法）：
   `updateSectionStatus` / `propagateLightSources` / `checkBlock`。
   原版 `ThreadedLevelLightEngine.initializeLight()` 与 `lightChunk()` 就是这套组合。
2. **天光柱高图是独立缓存**，不会自动跟随方块变化 ⇒ 必须 `chunk.initializeLightSources()`
   （= `ChunkSkyLightSources.fillFrom(chunk)`）**先重建**。
   `ChunkStatusTasks:163` 在区块加载的 INITIALIZE_LIGHT 阶段调的就是它。
3. **同步给客户端的机制是自动的**：`LayerLightSectionStorage.swapSectionMap`
   → `chunkSource.onLightUpdate` → `ChunkHolder.sectionLightChanged` → `broadcastChanges`
   发 `ClientboundLightUpdatePacket`。所以**不需要**自己造光照包。
   ⚠️ 而且 `ThreadedLevelLightEngine` 上这几个入口是**入队**（`lightTasks`）而非同步执行 ⇒
   我们紧跟着发的 `notifyChunkClients` 里那份光照**当帧仍是旧的**，下一 tick 被覆盖。

### 改动

`ExplosionComponent`：
- 新增 `refreshLightAfterBulkEdit(level, chunk, removedLightSources)`（**包级可见 = 可测性接缝**，
  与 `applyToChunk` 同一理由）：
  ① `chunk.initializeLightSources()` 重建天光柱高图
  ② 逐 section `updateSectionStatus(SectionPos.of(chunkPos, minSection + i), section.hasOnlyAir())`
  ③ `lightEngine.propagateLightSources(chunkPos)`
  ④ 被炸掉的发光方块逐个 `checkBlock(pos)`（减光）
- `applyHighYieldToChunk`：收集阶段顺手记 `state.getLightEmission(level, pos) > 0` 的位置
  （**零额外扫描成本** —— state 本来就在手里）。
- `deleteChunkContent`：传 `List.of()`（整区块清空 ⇒ 每个 section 都变空，旧光随 ② 丢弃）。
- 文件头注释补上「后两种绕过原版回调 ⇒ 必须补光照刷新」。

### 两条红线（写反了等于没修）

1. **①必须早于③** —— ③ 是**按柱高图**算天光的；高图还是旧的，算出来还是黑的。
2. **③只管增光、④才管减光** —— `propagateLightSources` 只把**现存**光源重新登记；
   光源方块被炸掉后残留的旧光，只能靠 `checkBlock` 降下来。

### 测试

- 新增 `ExplosionComponentLightTest`（**5 项**）：钉"接线与顺序" ——
  四入口顺序（`InOrder`）/ section 空态按 `hasOnlyAir` 上报 / **section Y 用 `minSection + i`
  而不是索引**（世界最低段为负时会错位）/ 发光方块逐个 `checkBlock` / 无发光方块时不空转。
- 真实光照计算**只能游戏内验**（单测基础设施是 Mockito 替身，没有真 `ServerLightEngine`），
  所以用例定位是**接线守卫**，与技术文档口径一致。
- 全量 **348 用例全绿**（原 343 + 5）。

### 文档同步

- `living-tnt-tech.md` §4.3：HIGH_YIELD/SUPER 流水线补「光照刷新」阶段 +
  新增 **§「⚠️ 光照刷新（2026-09-22 修）」**（根因表 / 四步 / 两条红线 / 异步同步机制）。
- `living-tnt-testing.md`：`T-05`、`T-06` 的期望补「**炸完坑里是亮的，不是漆黑一片**」
  （这两条正好是被修的两个模式）—— **不加新编号**，避免又一轮重排。
- `file-map.md` 测试树补 `ExplosionComponentLightTest`；`AGENTS.md` 测试数 343 → 348 +
  新增 `(2026-09-22)` 进展条目。
- `tools/doc_check.py` **六项全过**（入口体量 19744 / 20000）。

### ⚠️ 发现的规约冲突（未擅自改，留给用户决定）

`docs/README.md` §4 触发条件是「入口体量 > 20,000 **或**开发进展 > 3 个批次」，
动作是「保留最近 3 个批次，其余迁出」。但现在**做不到**：

- 连续性校验（`doc_check.py`：`CONTINUITY_MAX_GAP_DAYS = 1`）要求
  `changelog 最新日期` 到 `AGENTS 最早日期` **间隔 ≤ 1 天**。
- 现有批次是 09-16 / 09-18 / 09-20 / 09-22（**相隔 2 天**，中间没有 09-17、09-19、09-21 的批次）。
- ⇒ 迁出 09-16 后：changelog 最新 09-16、AGENTS 最早 09-18 ⇒ **空档 2 天，校验 FAIL**。
  迁出更多只会更糟（09-18 → AGENTS 最早 09-20，仍是 2 天）。
- **结论**：在批次间隔 > 1 天时，「3 个批次」与「空档 ≤ 1 天」**互相矛盾**。
  当前保留 4 个批次（校验全过，体量 19744/20000 未超）。
  可选解法：把容忍空档放宽到 ≤ 3 天，或把「批次」改成按"有内容日期"计。
  **这是规约缺陷，不是本次改动引入的** —— 需要用户定调。

## 用户定调：删掉「空档 ≤1 天」（同日追加）

用户原话：「**开发空档是什么意思喵。我又不是每天都开发。相关文档规则更新一下喵。**」

⇒ 我上一轮把这条当成"两个规则的矛盾"报上去，**方向报偏了**：
它根本不是"两条规则打架"，而是**这条规则本身是错的** ——
它把「没开发」当成了「漏记」的证据。**日期间隔本身不含信息**。

### 四处同步改（规约必须一处不漏）

| 位置 | 改动 |
|---|---|
| `docs/README.md` §4 步骤 5 | 「连续性校验：间隔 ≤1 天」→ **「边界单调性校验：`changelog 最新` 必须早于 `AGENTS 最早`」** |
| `docs/README.md` §7 检查表 | 「4. 归档连续性 / 空档 ≤1 天」→ **「4. 归档边界 / 单调，不校验间隔天数」** |
| `docs/README.md` §4 步骤 2 | **新增**「从最旧批次开始**连续迁出**，不许跳批次」（防丢历史的正解） |
| `docs/README.md` §4 步骤 5 | 守恒校验标注为**防丢历史的"主"手段** |
| `docs/archive/changelog.md` 节首 | 同步改写（原文也写着"必须校验日期连续性…不得有空档"） |
| `AGENTS.md` 节首「归档规则」 | 同步改写为三条校验（连续迁出 / 守恒 / 边界单调） |
| `tools/doc_check.py` | `CONTINUITY_MAX_GAP_DAYS = 1` → `ARCHIVE_WARN_GAP_DAYS = 30`；`check_continuity` 重写 |

### 新判据（可复算）

- **FAIL**：`changelog 最新日期` **不早于** `AGENTS 最早日期` ⇒ 归档边界切错（重复/逆序）。
- **WARN（不阻断）**：边界相隔 > **30 天** ⇒ 提示「确认这段没只在 `.workbuddy-ai/memory/`
  日志里、而没进过 AGENTS/changelog 的工作」。一个月没开发完全正常，别读成失败。
- **明确写进文档**：「**不要再把『空档 ≤N 天』加回来**」——防止未来会话"好心"加回。

### 验证

- **负向测试**（证明校验不是空的）：临时往 changelog 注入 `## 2026-12-31`
  ⇒ `4. 归档边界 : FAIL（2026-12-31 未早于 2026-09-18）` ✅；还原后 OK ✅（临时文件已删）。
- 借此**归档 `2026-09-16` 批次**（新规则下可行了）：`AGENTS.md` **19744 → 17672 字符**，
  批次回到 **3 个**（09-18 / 09-20 / 09-22）。
- **守恒校验逐段判定**：changelog 新增段 **46 个非空行 / 3 条 bullet**，
  12 个特征串（`Tillables`/`FakeHoe`/`Registry is already frozen`/`313 用例全绿`/
  `LivingFarmlandSeedDecorator`/`297`/`LivingFarmlandPlacement`/`293`/`§12.1 实测清单`…）
  **全部命中**；AGENTS 侧已无 09-16 批次。
- `doc_check.py` **六项全过**：`4. 归档边界 : OK（2026-09-16 → 2026-09-18，相隔 2 天）`
  —— **"相隔 2 天"现在是 OK**，正是本次修订的目的。

## 用户报告：大当量爆炸坑不圆 + 一个方形区域"有渲染没方块"

用户原话：「**炸出的坑有些地方不是圆的，会有一整个区块的部分保留。**
**然后爆炸后会有一个正方形的区域保留着方块的渲染，但实际已经没有方块了。**」

### 问题一：`affects()` 判据错（**已修，单点根因**）

`ExplosionParams.affects(ChunkPos)` 用的是「**区块中心**到爆心距离 ≤ radius」，
但区块是 **16×16** —— "中心在半径外、边缘却落在球内"的区块大量存在
（区块中心最远可比最近角远约 11 格）。这些区块在 **`schedule()` 建档时就被标记成"已完成"**、
**永不处理** ⇒ **整块地形留着 ⇒ 坑不圆**。

该判据在**三处**生效（建档标记 / `flush` 跳过 / `remainingChunks()`）⇒ 单点根因。

**修法**：改为「**区块 AABB ∩ 球体**」——取区块 AABB 上离爆心最近的点算距离。
只判水平方向即可（区块是贯穿世界高度的柱体）。

⚠️ **它被测试钉住了**：`ExplosionParamsTest.affects_matchesLegacyCriterion` 用例名写着
"与旧 `scheduleSuperExplosion` 同口径"，注释还解释"保持这条判据不动是为了不改变既有行为"。
⇒ **错误行为被测试保护了起来**，这是它长期没被发现的原因。
已改为 `affects_isChunkAabbIntersectingSphere` + 边界回归
`affects_rimChunkWithCenterOutside_isAffected`（**用例内先断言"中心确实在半径外"作为前提**）。
账本侧同步：`ExplosionLedgerTest` 里 `5→9` / `5→9` / `4→8`，并新增
`schedule_marksTrulyOutOfCircleAsDone`（半径 1 → 3×3 里只有中心 1 格，其余 8 格建档即完成）。
`flush_isBudgeted_remainderCarriedOver` 原假设"两轮炸完"不成立（受影响数 49→69），
改为**推进到收敛**（别写死轮数）。

> **教训**：**把"既有行为"当规格钉住之前，先确认既有行为是对的。**

### 问题二：**没能定位**（诚实记录，未编造修复）

「方形区域有渲染但没方块」= 客户端幽灵方块。我**逐段核了整条客户端链路**，逐条**排除**：

| 假设 | 核实结果 |
|---|---|
| 整区块包对已加载区块**不重建渲染** | ❌ **不成立** —— `ClientPacketListener.enableChunkLight()` 对**每个 section** 都调了 `setSectionDirtyWithNeighbors` |
| 包的 section 序列化是稀疏的（跳过空 section） | ❌ 不成立 —— `ClientboundLevelChunkPacketData.extractChunkData` **遍历所有 section**，无 `hasOnlyAir` 跳过 |
| 光照位集 `null` 导致包不完整 | ❌ 不成立 —— `skyLight == null` 即"全部 section" |
| `getPlayers(cp, false)` 漏掉边界区块 | ❌ 不成立 —— `contains(x,z)` 走 `includeOuterChunksAdjacentToViewBorder=true` |
| `setBlockState(useLocks=false)` 不维护计数 | ❌ 不成立 —— `nonEmptyBlockCount` 照常增减，`hasOnlyAir()` 准确 |
| 客户端 `replaceWithPacketData` 拒绝已存在的区块 | ❌ 不成立 —— 走 `levelchunk.replaceWithPacketData(...)` 原地替换 |

⇒ **不能按被推翻的假设去"修"**（那正是用户批评过的"乱编"）。
已把「整区块包会重建渲染」这条结论写进 `AGENTS.md` 的进展条目，**免得下次重走**。

**待用户澄清的判据**（下次问）：那个方形区域**有碰撞吗**？
- **有碰撞、挖下去有方块** ⇒ 是问题一（整块残留），本次修复应已解决；
- **能走进去、无碰撞** ⇒ 才是真·幽灵方块（客户端数据陈旧），需要另查。

### 顺带

- **加固**：方块同步改到光照刷新**之前**发（`notifyChunkClients` → `refreshLightAfterBulkEdit`）。
  方块同步是用户直接看得见的；光照是入队的、原版随后会用 `ClientboundLightUpdatePacket` 覆盖 ⇒
  不该有机会挡住方块同步。
- **记录一个真缺口（未修）**：高度图也没随批量修改更新（绕过 `LevelChunk.setBlockState` 的副作用），
  且 `WORLD_SURFACE`/`MOTION_BLOCKING` **会随区块包发给客户端**。
  `Heightmap.primeHeightmaps()` 是唯一重建入口，但在分帧预算下逐区块调用有实打实的 CPU 成本
  ⇒ **先量化再决定**，已写进 `living-tnt-tech.md` §4.3「已知缺口」。
  ⚠️ 记下这条是因为**差点顺手加上去**——修显示问题换来掉 TPS 是净亏。
- 文档：`living-tnt-tech.md` §4.3 新增「⚠️『哪些区块受影响』的判据」小节；
  `file-map.md` 测试项数同步（`ExplosionParamsTest` 4→6、`ExplosionLedgerTest` 9→10）；
  `AGENTS.md` 测试数 348→351 + 进展条目。
- 验证：全量 **351 用例全绿**；`./gradlew build` 通过；`doc_check.py` **六项全过**（入口 18659/20000）。

## 续：把「坑必须是圆的」变成不变量测试（同日追加）

**动机**：修好 `affects` 只是"当前对了"，没有东西拦着它再错回去。
而它**上一次错回去时是被测试钉住的** —— 所以必须补一条**不依赖实现**的不变量。

### 新增守卫

`ExplosionParamsTest.affects_coversEveryBlockInsideSphere`：
遍历半径内**每一个方块**，断言它所在区块必须 `affects == true`
（按区块缓存，避免重复判定）。

- 这条断言的是**性质**（"球体被处理区块完全覆盖"），不是"中心/最近点怎么算" ⇒
  换个正确实现它照样过，换个错误实现它就炸。
- 用例内附**前提断言** `checked > 3000`，防止球体范围被改小后测试变成空转。

### ⚠️ 关键一步：**负向验证**（"能通过" ≠ "能抓住"）

把 `affects` **临时改回旧的「区块中心」判据**，跑该用例：

```
> Task :test FAILED
```

⇒ **确认它在旧实现下会真的 FAIL**，不是空测试。随后还原（`grep -c getMinBlockX` = 1 核对），
临时备份已删。**这一步不能省** —— 只报"新用例 PASSED"说明不了任何事。

### 量化（纯几何，可复算）

| 半径 | 网格 | 旧命中 | 新命中 | 被漏掉 |
|---|---|---|---|---|
| 16 | 3² | 5 | 9 | 4 |
| 64 | 9² | 49 | 69 | 20 |
| 235 | 31² | 673 | 741 | **68** |
| 333 | 43² | 1361 | 1447 | **86** |

⇒ 每场大当量爆炸有**几十个区块**被整块跳过 = 几十块方形地形留在坑边缘，
与玩家描述的"有些地方不是圆的、一整个区块的部分保留"完全吻合。

### 验证

- 全量 **352 用例全绿**（351 + 1）；`doc_check.py` **六项全过**（入口 18807/20000）。
- `file-map.md` 测试项数同步（`ExplosionParamsTest` 6→7）；
  `living-tnt-tech.md` §4.3 补漏块量化表 + 守卫用例说明；`AGENTS.md` 351→352。

## 用户补了一句关键信息 ⇒ 问题二**定性完成**（同日追加）

用户原话：「**而且过一会就会自己消失了**」

**这一句就把问题二定性了** —— 不用再猜"客户端数据陈旧"：

| 现象 | 性质 |
|---|---|
| 方形区域**过一会自己消失** | **客户端重建排队**（数据已对，只是还没重画） |
| 方形区域**一直都在**、有碰撞、挖下去有方块 | **bug**（服务端根本没处理该区块 = 问题一） |

⇒ **问题二 = 正常现象，不是 bug。** 之前我 6 个假设全被源码排除、找不到"幽灵方块"机制，
正是因为**它本来就不是幽灵方块**。**用户的补充信息比我继续读源码有效得多** ——
以后再遇到"现象描述含糊"，**先问一个能区分的判据**（这里是"会不会自己消失 / 有没有碰撞"），
比继续埋头读代码划算。

### 机制（已核实源码）

```
notifyChunkClients → ClientboundLevelChunkWithLightPacket
  └─ 客户端 handleLevelChunkWithLight
       ├─ updateLevelChunk → replaceWithPacketData   // 方块**数据**立刻正确
       └─ queueLightUpdate → enableChunkLight
            └─ 对**每个 section** 调 setSectionDirtyWithNeighbors(x, y, z)
                 // 每次 = 标记 3×3×3 = 27 个 section 需重建
```
一次大当量爆炸改动**上万个 section** ⇒ 重建队列要慢慢排，没轮到的仍画旧几何，排完即恢复。
（所以"挖下去是空气、能走进去"——数据是对的。）

**为什么不改用逐方块包**：`ClientboundSectionBlocksUpdatePacket` 只标记改动的那一个 section（无 27 倍放大），
但半径 235 要改几千万个方块，逐方块发包体积不可接受 ⇒ 只能整区块打包，代价就是重建风暴。

### 落点

- `living-tnt-tech.md` §4.3 新增小节：机制 + **两种现象对照表** + 可选优化（边缘区块改发
  `ClientboundSectionBlocksUpdatePacket` 以避开 27 倍放大 —— **未做，需先量化**）。
- `living-tnt-testing.md` §5「看着像 bug 其实不是」加一行 + **「两种要分清」对照表** ⇒ 免群友误报。
- 顺带把方块同步改到光照刷新**之前**发（方块是用户直接看得见的）。

### 顺手做：AGENTS.md 体例收敛

发现自己的进展条目写太长（把技术文档的正文抄了一份），违反 AGENTS.md 自己的体例
「**一条 = 一行结论 + 指针**，细节写进 `docs/tech/*.md`」。
⇒ 把 09-22 批次 4 条按体例收敛，**入口体量 19155 → 17836**（腾出 1319 字符）。
**这不只是省空间：抄一遍正文就多一份会漂移的副本。**
（当时距 20,000 预算只剩 845 字符，本来也该收敛。）

### 验证

- 全量 **352 用例全绿**；`doc_check.py` **六项全过**（入口 17836/20000）。

---

## 🚨 事故：.git 被清空 + 恢复（2026-09-22 14:20~14:35）

用户报告：「我发现项目里的 git 历史都没了」。**已恢复。**

### 事实（全部实测）

| 项 | 状态 |
|---|---|
| `.git/objects/` | ❌ **整个目录消失** |
| `.git/refs/` | ❌ **整个目录消失** |
| `.git/logs/`（reflog） | ✅ 完好，**276 条**，是丢失提交的唯一线索 |
| `.git/{config,HEAD,index,COMMIT_EDITMSG,ORIG_HEAD,FETCH_HEAD}` | ✅ 完好 |
| **工作区** | ✅ **完好**（284 个 java / 444 个文件） |
| 远程 `777hi/livingitem-template-1.21.1` | ✅ **198 个提交完整** |

`.git/` 目录 mtime = **Sep 22 10:57**；最后两条本地提交时间戳 = **10:51:11**。
⇒ 事故发生在 **今天 10:51~10:57**，**早于本次会话**（本会话上次操作是 00:07）。

### 丢失的内容（仅 3 项，且内容未丢）

- 2 个提交：`82e1f699`「活武器抽象：补全右键的「空气/物品」分支」、
  `74211eaff`「文档：活武器「攻击方式的逻辑」设计（idea.md）」—— **从未推送**
- 1 个 stash：`3248c765`「On master: renderer-WIP-2026-09-21」
- ⭐ **它们的文件内容全部保留在工作区** ⇒ 现表现为相对 `e7babd6` 的 **19 处未提交变更**，
  **零内容损失**。

### 恢复步骤（可复用）

1. **先备份受损 `.git`** → `.workbuddy-ai/backup/git-damaged-20260922-142059/dot-git`
   （reflog 是唯一线索，**必须先备份再动手**）
2. `git clone --bare <remote> <临时目录>`（**4.5 MB**，18 秒）
3. `cp -r <clone>/objects .git/objects` + `cp -r <clone>/refs .git/refs` + `cp <clone>/packed-refs .git/packed-refs`
4. `git fetch origin` 重建远程跟踪引用
5. `git remote set-url origin https://github.com/777hi/livingitem.git`
   ⚠️ **仓库已改名**：`livingitem-template-1.21.1` → **`livingitem`**（GitHub API 返回
   `Moved Permanently`；两个地址 `ls-remote` 都指向同一提交 `e7babd6`）
6. 验证：`git rev-list --count HEAD` = **198** ✓

### ⭐ 根因线索（重要，未最终定论）

**做受控实验时发现：`.git` 下的写入存在「延迟可见性」** ——
`git update-ref refs/remotes/origin/master <sha>` 返回 0，但**立即**用 `test -f`、`ls`、
甚至 `git show-ref` 都查不到；**约 10 秒后文件才出现**。
⇒ 不是"被删除"，是**写入延迟可见**。文件系统本身可写（手动 `mkdir`+写文件立即成功）。

**这个行为足以解释原事故**：git 的维护类操作（`gc` / `repack` / `fetch --prune`）
会「写入后立刻读回」，一旦读不到就会判定对象**不可达** ⇒ **prune 掉 `objects/`**，
同时 `refs/` 被枚举为空。而 `config` / `HEAD` / `logs` / `index` 这类小文件不参与该流程 ⇒ 幸存，
**与实际幸存/消失的分布完全吻合**。

**三个加重因素（均已确认）**：
- ⚠️ **G 盘 98% 满**：544G 用 532G，**仅剩 13G**
- ⚠️ **OneDrive 正在运行**（`OneDrive.exe` / `OneDrive.Sync.Service.exe` / `FileSyncHelper.exe`）
  —— OneDrive 同步含 `.git` 的目录是**公认的 git 杀手**
- ⚠️ 仓库里有第三方工具痕迹：`.git/gk/`（**GitKraken**，最后访问 2026-09-04）、
  `.git/opencode`（另一个 AI 工具）

### 已做的防护

- `git config gc.auto 0` —— **禁用自动 gc**，防止再次被 prune
- 备份保留在 `.workbuddy-ai/backup/git-damaged-20260922-142059/`

### ⚠️ 遗留风险 / 待用户处理

1. **立即 push**（把工作区的 19 处变更推上去，脱离这块盘）
2. **清理 G 盘空间**（98% 满很危险）
3. **检查磁盘健康**（`chkdsk G:` / SMART）
4. **不要**在搞清楚前运行 `git gc` / `repack` / `prune` / `fetch --prune`
5. **`git fsck` 会报 reflog 错误**（`invalid reflog entry 87024953...` / `85d30cd7...`）——
   那些是被删掉的 `test` 分支/丢失提交的残留记录，属**无害噪音**
6. ⚠️ **观察到有其它进程在动这个仓库**：`git status` 里
   `.workbuddy-ai/backup/...` 一度显示为 **`A `（已暂存）**，但我从未执行过 `git add`
   ⇒ 有 IDE / 工具在自动 stage。**排查时要考虑多工具同时操作同一仓库的风险**。




