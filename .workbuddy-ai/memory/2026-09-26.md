# 2026-09-26

## 项目 git 管理体检（只读分析，未改动仓库）

体检结论：仓库整体健康（262 提交 / 512 跟踪文件 / `.git` 仅 10M / 单 pack + 769 松散对象 / 工作区干净），
但有几处可改进。**本轮未执行任何写操作**（除 memory 压缩）。

发现的要点：

1. **`master` 领先 `origin/master` 2 个提交**（`baece04`、`e20f767`）——未推送。G 盘有坏块的前提下，
   GitHub 是唯一脱离该物理盘的副本，建议立即 push。
2. **`.workbuddy-ai/backup/**`（12 文件）被跟踪**，但 `.gitignore` 已写 `.workbuddy-ai/backup/`
   —— 忽略规则对**已跟踪**文件无效，属历史遗留；这些是文档手术快照，git 本身已保存历史，属噪音。
3. **换行符策略不完整**：`core.autocrlf=true`，`.gitattributes` 只钉了 `src/generated/**`；
   索引里 CRLF 的文件有 `.gitignore` / `.gitattributes` / `.github/workflows/build.yml` /
   `.trae/skills/*` / `.codebuddy/plans/*` / `.workbuddy-ai/backup/*`。多工具写文件 ⇒ 幻影 diff 风险。
4. **并行 AI 会话共用同一个工作树** —— 这是文档里「`git add -A` 夹带别人改动」的结构性根因；
   分支无法隔离（同一 worktree 所有会话看到同一批文件）。结构性解法是 `git worktree` 给每个工具独立目录。
5. 其他：根目录残留 `_pinyin_data_fragment.txt`（213KB，最大跟踪文本）/ `_build_pinyin_helper.py`；
   `docs/crash-2026-09-24_20.58.52-client.txt` 崩溃日志进了 docs 树；`.zcode/`(8) 与 `.trae/`(6) 被跟踪；
   262 提交**零 tag**；`.git/hooks` 为空（`tools/doc_check.py` 靠人记得跑）；无 stash、无额外分支/worktree。

（本日志为「git 管理建议」的现场记录，不是交付物。）

## 顺手：`MEMORY.md` 超限截断 ⇒ 已压缩

- 症状：`.workbuddy-ai/memory/MEMORY.md` 达 ~11.7KB，注入时被截断（末尾「电力层/PerfMetrics」曾丢）。
- 处理：按该文件自身声明的设计（「只保留红线速记 + 指针」）重写为一行式红线 + `docs/` 指针，
  压到 **4,700 字节**（约 40%）；补上「G 盘诊断三招」「Pillow 隔离 venv 路径」两条工具侧独有信息。
- ⚠️ 结论：该文件**每轮都进上下文**，条目要按「一句话 + 指针」写，别把正文搬进来。

## 「哪些文件不该进 git」盘点（只读，未执行）

- **第一档·确凿该移出（20 文件 ≈ 640KB）**：
  `.workbuddy-ai/backup/**`(12, 500KB —— `.gitignore` 里本就有 `.workbuddy-ai/backup/`，忽略对已跟踪文件无效) ·
  `.workbuddy-ai/patches/2026-09-11-overflow-fix-*.patch`(已进代码：`LivingWaxedCopperFunction:864`
  的 `PowerMath.mulDivFloor`，且 `git apply --reverse --check` 干净通过 ⇒ 冗余) ·
  `docs/crash-2026-09-24_20.58.52-client.txt`(66KB，`.gitignore` 只挡 `*.log`) ·
  `.zcode/plans/plan-sess_<uuid>.md` ×5 + `.codebuddy/plans/*_<hash>.md`(会话级草稿)。
- **第二档·跨工具重复且已漂移（9 文件，待用户拍板）**：`.trae/skills/**`(6) 与 `.zcode/skills/**`(3)；
  实测 `mc1-21-1--` / `neoforge-docs` 两边**完全相同**，`vanilla-jar` **已不同**（4080 vs 4264 字节）；
  且 WorkBuddy 侧 `~/.workbuddy-ai/skills/` 也有同名三个 ⇒ **同一份技能存在三处并开始漂移**。
- **有意跟踪、建议保留**：`.workbuddy-ai/memory/**`（16 文件 ≈ 370KB）。`.gitignore` 只忽略
  `backup/` 与 `tmp/` ⇒ memory 是**有意**跟踪的；G 盘坏块下这是一层合理备份，不动。副作用要知晓：
  工具侧日志成了项目内容，15 份日报 370KB，且与 `docs/changelog.md` 职责重叠。
- ⚠️ **反例纠正**：`_pinyin_data_fragment.txt` / `_build_pinyin_helper.py` 看着像根目录杂物，
  但 2026-09-03 日志明确写着**「按用户要求保留」** ⇒ **别再建议删这两个**。
- 明确不动：`gradle/wrapper/gradle-wrapper.jar`(`.gitignore` 的 `*.jar` 已用 `!` 正确豁免) ·
  `gradlew`/`gradlew.bat` · `logo.png` 与全部纹理 · `docs/**` · `.github/workflows/build.yml`。
