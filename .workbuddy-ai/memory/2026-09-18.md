# 2026-09-18 工作日志

## 修复：Create 跨区块传送带 → 服务端线程死锁

**报告**：living_item 1.3.2 + Create 6.0.10，进世界即服务端线程卡死（GUI 能动、不能合成、不能 tp、
玩家掉虚空）；单个传送带不卡，跨区块传送带必卡。
堆栈 `ContainerChunkCache.onChunkLoad → BeltBlockEntity.initializeItemHandler → ServerChunkCache.getChunk`。

**根因（已用原版/NeoForge 源码逐条确认）**：

- `ChunkEvent.Load` 在 `ChunkStatusTasks.full()` 的区块 FULL 任务**内部**触发（主线程）。
- 我们在事件里做 `level.getCapability(...)` → 执行到 Create 传送带的 provider →
  `initializeItemHandler()` → `level.getBlockEntity(controller)`（**别的区块**）→
  `ServerChunkCache.getChunk(..., requireChunk=true)` → `managedBlock` + `join`
  等一个"只能由主线程自己推进"的 future ⇒ 自等自。
- NeoForge 在 `ChunkEvent.Load` javadoc 里已明确警告"不延迟世界交互会死锁"。
- 同区块不卡的原因：原版 `ChunkHolder.currentlyLoading` 旁路只覆盖"正在加载的那个区块本身"。
- 附带的坑：Create 的守卫 `level.isLoaded(controller)` **不充分** —— `hasChunk` 只查 ticket level。

**改动**：

- `ContainerChunkCache.onChunkLoad` → 只登记坐标（唯一 level 调用是 `dimension()`），
  扫描全部推迟到 `flushPendingRescans`（`ServerTickEvent.Pre`）。
- `flushPendingRescans` 新增每 tick 限量 `MAX_RESCANS_PER_TICK = 64`，剩余顺延不丢。
- 新增 `ContainerChunkCacheChunkLoadTest`（4 项），全量 **317 passed / 0 failed / 0 skipped**。

**文档**：infrastructure §3.2 加红线 + 修两处旧口径（`ServerTickEvent.Post`→`Pre`、
`onChunkUnload` 曾写"即时移除"）；decisions `D-core-04`；file-map 测试树；AGENTS 进展 + 基线 317。

**顺带完成文档规约 §4 的归档**（入口已 20,759 > 20,000 且进展达 4 批次）：
09-14 批次迁入 changelog（逐行守恒校验通过），入口 20,759 → 19,653，`doc_check.py` 0 警告。
备份在 `.workbuddy-ai/backup/2026-09-18-chunk-load-deadlock/`（提交进 git 后可删）。

## 沉淀

- 新建工具侧技能 `mc-event-callback-safety`（判定事件回调/注入点能否碰世界 + 卡死定位 + 测试手法）。
- 项目级事实落在 `docs/system-design/living-item-infrastructure.md` §3.2 与 `D-core-04`，
  技能只做适配层并指向它们。

## 追问：「强制同步加载」的量化（同日）

用户追问这个概念，顺手把三个数字从源码里挖实并写进 §3.2：

- `ChunkPyramid.GENERATION_PYRAMID`：`FULL` 的累积依赖 = **`STRUCTURE_STARTS` 半径 8**
  ⇒ 一次 `getChunk(FULL, true)` 的依赖足迹是 **17×17 = 289 个区块**。
- `TicketType.UNKNOWN` lifespan = **1**，`Ticket.timedOut` = `currentTick - createdTick > 1`，
  `purgeStaleTickets` 每 tick 一次 ⇒ 票据约 2 tick 后失效、区块被卸载
  ⇒ **每 tick 触发 = 每 tick 重新生成**（比"强制加载"更严重的是"反复加载"）。
- `max-tick-time` 默认 1 分钟且 `ServerWatchdog` 只在 `DedicatedServer` 启动
  ⇒ 专用服务器会判崩，单人存档纯卡死（对应用户现象）。
- `ServerTickEvent.Pre` 由 `EventHooks` 从 `MinecraftServer.tickServer` **开头**触发
  （未进 `tickChildren`/区块流水线）⇒ tick 阶段做同步加载合法，只是慢。

**残余风险（未防护，用户未表态）**：`processLevelContainers` 每 tick 对每个 BE 调
`getCapability` —— 若某模组 provider 伸手到未加载区块，就会变成每 tick 强制加载+卸载。
已实测的 Create 传送带有 `isLoaded(controller)` 守卫，不触发。排查触发条件：
spark 里 `getChunkCacheMiss`/`chunkLoad` 异常高。已写进 §3.2。

**顺带修文档缺陷**：`living-item-infrastructure.md` 里 `### 10.1 性能判读…` 被追加在
`## 12. 关键文件索引` 底下、且编号与 §10.1 重复 —— 已移回 §10 并改号为 **§10.3**
（守恒校验通过），同步更新 `docs/README.md` §8 与本文件 MEMORY.md 的引用。

## 追问二：强制加载的入口清单与概率（同日）

用户判断「只有活地图传送主动加载，其余被动」——**不准确**。全量排查后新增 §3.2.1。

**明确主动 `getChunk` 的**：`TeleportHelper`（活地图传送）、`SableIntegration.teleportSubLevel`
（飞艇）、`ExplosionComponent`（3 处，由爆炸区域读方块间接触发）。

**结构性根因（`ChunkLevel` 常量）**：原版 BE 只在 **≤31（ENTITY_TICKING）** 跑 ⇒ 邻居 ≤32
⇒ 必然已加载 ⇒ 读邻居免费。本框架用 `getChunkNow` 拿 **≤33（FULL_CHUNK_LEVEL）** 区
⇒ 容器落在 **33 圈（已加载但不 tick）** 时邻居落在 **34+（未加载）** ⇒ 强制加载。
**危险带 = 最外一圈区块**（视距 12 时 625 中的 96 个）。

**关键事实**：`BlockCapability.getCapability` 内部先 `level.getBlockState(pos)` 再
`getBlockEntity(pos)` ⇒ **能力查询本身就是加载入口**。

**入口清单**（详见 §3.2.1 表）：`ContainerRedstoneData.sampleFaceInput`（getSignal，红石系）、
`CrossContainerTransfer.getNeighborHandler`（活漏斗）、`processContainerAt`（大箱子另一半）、
`StressOutputManager`/`CreateIntegration`（活水车下方一格）、`ExplosionComponent`。
**已守卫**：`LivingEnderChestAccessor`、`ContainerEnergyStorage`（只读自身 pos）、`flushPendingRescans`。

**更正上一轮的说法**：`addTicket` 会 `addOrGet` + `setCreatedTick` **续期** ⇒ 连续每 tick 触发是
**永久钉住该区块**，不是"每 tick 重新生成"。反复重生成只在间歇触发/来回卸载重载时。已在 §3.2 改正。

**概率结论**：小几率但非零，且**移动时概率显著上升**（加载区边缘会扫过基地，逐个容器进入危险带）。
命中后果严重且长期：首次可能真生成（289 区块足迹）+ 永久钉住 + 理论逐圈外扩（未实测）。

**仍未防护**（用户未表态）。若要做，最小方案是在读邻居前统一加 `isLoaded`/`hasChunk` 守卫
（把危险带里的容器降级为"这一 tick 不读邻居"），而不是引入缓存或轮询。

## 追问三：概率更正为 100% + 后果清单（同日）

用户指出「取决于玩家活动半径 ⇒ 百分百」。**同意并更正**：加载区边界随玩家移动扫过世界，
任何需要读邻居的活物品容器迟早被扫进危险带 ⇒ **不是"会不会"而是"多频繁/是否恶化"**。
已把 §3.2.1 里"小几率"的口径改掉。

后果清单已写进 §3.2.1（7 条）：① 单 tick 卡顿（289 足迹，未探索区域是真生成）；
② 移动中周期性卡顿；③ 邻居被**永久钉住**（票据续期）+ 内存/存档/IO/重进变慢；
④ **语义漂移**——活物品在"已加载但不 tick"区块里继续工作（原版 BE 只 ≤31 跑）；
⑤ 逐圈外扩（未实测）；⑥ 诊断难（PerfMetrics 盲区）；⑦ **传染**——同步加载会让其他模组的
Load 处理器在"主线程阻塞于 managedBlock"时运行，若它们也做世界交互就撞同一个死锁
（修复后我们自己的 onChunkLoad 只登记，反而消除了"scan→加载→Load→scan"的递归重入）。

**注意**：④ 其实是个独立的架构问题 —— 本框架把活物品的"工作范围"从原版的 ticking 区（≤31）
扩大到了 loaded 区（≤33）。这条值得单独评估，不只是强制加载的副作用。

## 追问四：给出候选修复方向（同日，未定案）

已把候选方案写进 §3.2.1（明确标注"未定案，只记分析"）。要点：

- **方向 A（推荐）**：处理范围判据 `getChunkNow() != null` → `isPositionTicking(chunkPos)`。
  **差别恰好只有最外一圈**（`isPositionTicking` = `FullChunkStatus.BLOCK_TICKING` = 32，
  `getChunkNow` = FULL = 33），即精确切掉危险带。收益：7 个入口全自动安全 + 外扩必然在 1 圈停 +
  消除语义漂移。代价：最外一圈活物品停摆。已核实 `ServerChunkCache.isPositionTicking` 是 public、
  `shouldTickBlocksAt` → `DistanceManager.inBlockTickingRange`（**不受 tick 冻结影响**）。
- **方向 B**：爆炸路径（半径 >1 格，A 覆盖不到）→ 对齐原版"越界方块跳过"，用现有分帧机制承接。
- **方向 C**：逐处 `isLoaded`，或传输层用 `BlockCapabilityCache`
  —— 已核实其 `getCapability()` 内部就是 `if (!level.isLoaded(pos)) { cachedCap = null; }`
  **自带守卫、不强制加载**，且失效自动重查；代价是生命周期管理（不能每 tick 新建）。
- **不可控面**：第三方 provider 可以伸到任意距离，A 覆盖不了，只能记录。
- **可观测性**：`ChunkSource.getLoadedChunksCount()` 是 public ⇒ 建议与 A 同批加到 `logCacheStats`。

**未实施任何代码改动**（等用户拍板）。若拍板 A，改动面是 `LivingItem.processLevelContainers`
的判据 + `ContainerChunkCache.cleanupStaleEntries` 的清理判据（现按 `getChunkNow`）。

## 追问五：越界爆炸的处理逻辑（同日，已写 §3.2.2）

**暴露面比"读邻居"大得多**：`radius = 4.0 × √TNT数` ⇒ 64 TNT 半径 32（5×5 区块）、
128 TNT 半径 45（7×7）、3456 TNT 半径 235（**31×31 = 961 区块**）。
未加载区块被 `getBlockState` 读到 ⇒ 每个 289 足迹 ⇒ 叠加爆炸自身 O(r³) 直接打爆 tick。

**口径定为：爆炸只作用于"当前已加载的世界"，未加载区块整块跳过。** 最有力的论据是
**超级爆炸模式本来就是这么做的**（`tickAll` 里 `if (level.hasChunk(...)) deleteChunkContent(...)`）
⇒ 这不是新增限制，而是**统一三个模式的口径**。另外原版"不越界"靠的是"中心在 ticking 区 +
半径小"，不是靠跳过，所以我们不能照抄"对齐原版"这个说法。

**4 处改动**：① 普通模式收集循环 ② 大当量阶段1 收集循环（各加 chunkPos 判定缓存，
`Long2BooleanOpenHashMap.computeIfAbsent`）③ 阶段2/3 的 `getChunk` → `getChunkNow` 防御性 ④ 跳过量记日志。

**已确认不用改**：`applyExplosionDamage` 的 `level.getEntities(AABB)` —— 1.21 走
`LevelEntityGetter`（已加载实体索引），**不碰区块**（我原本怀疑它会强制加载，验证后否掉了）；
`scheduleSuperExplosion` 纯几何算区块列表；`tickAll` 已有 `hasChunk` 守卫。

**fastutil 坑**：`Long2BooleanOpenHashMap` 默认 `defaultReturnValue(false)` ⇒ 用 `get()` 查
未判定过的 key 会返回 false（误当"未加载"）⇒ 必须 `computeIfAbsent` / `containsKey`。

**顺带发现的独立隐患**：radius 235 时收集循环 `471³ ≈ 1.05 亿次` 迭代，**即使全已加载**也是
主线程巨长操作 ⇒ 建议把"几何算区块列表 + 分帧"范式前移到普通/大当量模式。已记进 §3.2.2。

## 实施：强制加载防护（方向 A）+ 可观测性（同日，已完成）

用户拍板：先做防护与可观测性，**爆炸部分留待继续探讨**（未动）。

**代码改动（3 个主文件 + 测试）**：

- `ContainerChunkCache.getCachedChunks(ResourceKey<Level>)` →
  **`getProcessableChunks(ServerLevel)`**：快照之上新增 `isPositionTicking` 过滤。
  javadoc 写清"为什么必须过滤 33 圈"+"ticking 区块 3×3 邻域必为 FULL 所以可证明安全"+
  "**扫描不做此过滤**（ticking 提升无事件，过滤会永久漏发现）"。
- `LivingItem.processLevelContainers`：改用 `getProcessableChunks(level)`（1 行 + 注释）。
- `ContainerChunkCache.logCacheStats()`（死代码，无调用者）→
  **`describeCacheStats(ServerLevel)`**：输出 `缓存区块 / 可处理 / loaded区块 / 视距基准`。
- `ContainerMonitorCommand`：新增 `/living_monitor cache`（同时写 `ModLog.CONTAINER.info`）。
- 测试：`ContainerChunkCacheChunkLoadTest` 4 → **6 项**（新增 ticking 过滤 + 可观测性）。
  全量 **319 passed / 0 failed / 0 skipped**。

**文档同步**：§3.1/§3.2（两条判据）/§3.2.1（方向 A 标记已实施 + 修正"验证方法"为真实命令）
/§3.2.2（爆炸待定）/decisions `D-core-05`（20 条）/file-map/AGENTS 进展 + 基线 319；
`living-map-ender-pearl-tech.md` 的历史记录加了"方法已更名"的指针（旧名 grep 不会误导）。
入口体量一度 20,777 → 把 09-18 条目压成索引体例（细节都在 §3.2.x）→ **19,756** ✓。

**关键不变量（新）**：**发现 ≠ 处理** —— 发现覆盖所有已加载区块，处理只针对 ticking 区。

**技能**：`mc-event-callback-safety` 增补「修法二：碰哪些区块 —— 处理范围必须限定 ticking 区」
（危险带只有一圈 / isPositionTicking 的 3×3 证明 / 发现vs处理两条判据 / 可观测性 / 票据续期）。

## 文档体量：入口瘦身（用户拍板「清冗余，预算不动」）

用户提议把 AGENTS.md 预算从 20,000 上调。**先量分布再表态**，数据否掉了"预算不够"这个诊断：

| 章节 | 字符 | 占比 |
|---|---|---|
| 开发进展 | 6,840 | 35% |
| **已完成功能** | **4,005** | 20% |
| 子系统索引 | 2,578 | 13% |
| 模块地图 | 2,500 | 13% |
| 架构概览 / 项目概述 / 文档导航 | 3,578 | 18% |

**「已完成功能」是纯冗余**（77 项、0 未完成）：① 与「子系统索引」**14/14 关键词 100% 重合**；
② 塞满实现细节（`POS_TO_CACHE_KEY` / 合因子公式 / 相位三元件 / 4 级 BFS…）—— 正是入口铁律 1
「能反推的不进入口」禁止的；③ **含过时数字**（"146 项测试"，实际当时已 319；doc_check 抓不到，
因为它只校验基线声明）。

**执行**（按 §5「降级而非删除」，脚本 + 逐行守恒校验）：
- 整章搬到 **`docs/reference/completed-features.md`**（标注快照、注明"数量是迁出时的值"）。
- 链接相对路径跟着改：`](docs/tech/…)` → `](../tech/…)`（**这个坑很容易漏**，doc_check 会抓）。
- 入口只留一句路由：「下方全部子系统均已实现（没列的就是没做）」+ 指针。
- **入口 19,756 → 15,896 字符**（余量 244 → 4,104），历史一个字没动。

**规约同步**：`docs/README.md` §4 补了第二条冗余案例 + 明确「**不要靠上调预算解决体量问题**」；
§5 补了「能力清单 → reference」这条降级范式；§1 参考层举例补上新文件。
**技能** `livingitem-doc-system` 补了「相对链接要跟着改」与「先量体量分布再动手」两条操作要点。

## 爆炸口径重议（同日，用户否决「跳过」）

用户两点：① 逐 tick 分帧用到其他两个当量**可行**；② **未加载区块不爆炸 ⇒ 语义残缺**。

**接受否决，并重新框定问题**：残缺的本质不是"少了几个方块"，而是**爆炸作用范围由加载状态
决定** —— 加载状态是实现细节，泄漏到了游戏语义。同一个 64 TNT 爆炸，玩家站的位置不同结果不同
⇒ 不可解释。目标应是"**范围由物理（半径）决定，同时不阻塞主线程**"。

**已核实 API**：`ServerChunkCache.addRegionTicket/removeRegionTicket` 都是 **public**，
`DistanceManager.addRegionTicket` 建票为 `ChunkLevel.byStatus(FULL) - distance`；
`TicketType` 是普通类**无注册表**，可自定义 lifespan，票据会自动过期 ⇒ 方案可行。

**三个候选**（写进 §3.2.2）：
- ~~跳过~~ ❌ 已否决
- **A. 异步预加载 + 分帧**（推荐）：几何算区块列表 → 未加载的 `addRegionTicket` 申请异步加载 →
  每 tick 检查就绪 → 处理 → 释放；复用 `SuperExplosionTask` 骨架；**必须限速**。
  代价：未加载区块会**真生成**。
- **B. 参数化账本 + 自然加载时应用**：只炸已加载 + 记「中心/半径/模式」参数化账本（几十字节）→
  区块**自然加载**时在 tick 阶段应用（符合两条红线）；`SavedData` 持久化。
  代价：零加载开销，但"走过去才炸"更难解释 + 需持久化（重启丢账又残缺）。

**顺带发现两个既有 bug（必须先修）**：
1. **`PENDING_SUPER_EXPLOSIONS` 服务端关闭不清理** —— `LivingItem.onServerStopped` 清了三个静态缓存
   但漏了爆炸任务，`ExplosionComponent` 连 `clearAll()` 都没有 ⇒ 单机换存档（不重启 JVM）
   会持有旧 `ServerLevel` ⇒ **内存泄漏 + 在已关闭 level 上继续操作**。任何跨 tick 状态方案都会加重它。
2. **`tickAll` 每 tick 只处理 1 个区块**（961 区块要 48 秒）且**跳过未加载时 `index++` 无条件推进、
   不重试** ⇒ 现状就是静默残缺的，三个模式口径还各不相同。

**另修一个我自己引入的文档结构缺陷**：§3.2.1/§3.2.2 当初插到了 §3.2 **中间**，把 §3.2 的尾部
（内部结构 / 容器检测方式 / 缓存清理机制 / 兜底清理）挤到了 §3.2.2 之后。已用脚本搬回
（守恒校验通过），结构恢复 §3.2 → 3.2.1 → 3.2.2 → 3.3。**教训：插入新小节前要先确认
「上一节的正文在哪里结束」，不能只按标题定位。**

## 爆炸口径再修正：转向原版模型（方案 B）⭐

用户提出：「原版 TNT 在未加载区块爆炸，需要玩家靠近加载区块后才会真的爆炸。这个逻辑有参考价值吗」

**已核实源码，用户观察准确，且机制更精确** —— 不是"靠近才爆炸"，而是
**引信冻结 + 随区块持久化 + 回来续走**：
- `ServerLevel.EntityCallbacks.onTickingStart/End` → `entityTickList.add/remove`；
  `ServerLevel.tick()` 只遍历该列表 ⇒ 区块不再 entity-ticking 时实体**移出 tick 列表**（不消失）
- `PrimedTnt.addAdditionalSaveData/readAdditionalSaveData` 把 `"fuse"` 存进区块 NBT
- 玩家回来 → 区块重新 ticking → 实体恢复 → 引信**从冻结处继续** → 真的爆炸

**参考价值极大 —— 它推翻了我上一轮的推荐（方案 A 异步预加载）**：
原版给出了「**世界只在被观测的地方演化；未观测处的演化推迟到观测时，状态随区块持久化**」
这条现成语义。⇒ 我上一轮把"跳过"（永不发生）与"延迟到观测时"（最终一定发生）**混为一谈**了：
用户否决的是前者，而原版支持的正是后者。

**但不能照抄 —— 差异在账本的位置**：

| | 原版 TNT | 本模组 |
|---|---|---|
| 账本 | `PrimedTnt` 实体的 fuse 字段 | 活 TNT 的 DataComponent 引信 |
| 账本在哪 | **爆炸发生的那一个区块** | 容器所在区块（≠ 爆炸范围） |
| 爆炸范围 | 半径 4 ⇒ 必然已加载 | 半径可达 235 ⇒ 跨几十上百区块 |

⇒ 原版不需要账本机制；我们必须额外做一个**全局**账本（世界级 `SavedData` + 完成位图）。
**关键：不能挂中心区块** —— 玩家从半径边缘进入时中心可能不在加载区（视距 12 = 192 < 235 格）。

**修正后推荐 B**，三条理由：① 有原版先例（不是新设计，是把原版语义应用到爆炸范围）；
② **零加载开销**（A 会主动生成几百区块并永久留在存档 —— 原版从不这么做）；
③ **引信阶段已经天然符合原版**（活 TNT 引信在 DataComponent，容器只在 ticking 区被处理
⇒ 引信自动冻结）⇒ B 让"爆炸那一刻"也遵循同一模型，**整条链路口径统一**。

**B 的代价**：需 `SavedData` + 完成位图 + 与三种破坏方式对接；且原版有"引信还在闪"的预告
而我们没有 ⇒ 补一次 `playSound` + 粒子做可见性补偿。A 降为备选。

§3.2.2 已重写（新增「原版先例」小节 + 候选表加"原版先例"列 + 推荐改为 B）。

## 实施：爆炸逐区块分帧 + 待炸账本（方案 B，已完成）

用户拍板「按你的建议实施」⇒ 按 ①→④ 落地。

**新增 2 个类**（`living/components/`）：
- `ExplosionParams`（record）：中心/半径/模式/掉落模式 + **位图索引映射**（`bitIndex ↔ chunkAt` 互逆，
  固定网格枚举不按圆形筛 ⇒ 索引永远稳定）。`affects(cp)` 沿用旧 `scheduleSuperExplosion` 的
  "区块中心距 ≤ radius" 判据，保证行为不变。
- `ExplosionLedger extends SavedData`（世界级，随存档持久化）：参数 + `long[]` 完成位图。
  三种出队情形分清：**未加载 → 丢弃**（等 `ChunkEvent.Load` 重新登记，**不轮询**）；
  **预算用尽 → carryOver**（已加载的不会再触发 Load，丢了就永远不炸）；条目完成 → 移除。
  `MAX_CHUNKS_PER_TICK = 32`、`MAX_ENTRIES = 256`。

**`ExplosionComponent` 改造**：删掉三套"一次性遍历球体"（`executeNormalExplosion` /
`executeHighYieldExplosion` / `scheduleSuperExplosion` / `tickAll` / `PENDING_SUPER_EXPLOSIONS`），
统一为唯一入口 **`applyToChunk(level, params, chunk)`**（三模式各一个 per-chunk 实现）+
`forEachBlockInChunk`（只走球体在该列的 y 跨度，不扫整列）。`ignite()` 只做：
算参数 → 声光 → 实体伤害 → `ExplosionLedger.schedule`。

**接线**：`LivingItem.onServerTick` → `ExplosionLedger.flushAll`；新增
`LivingItem.onChunkLoad`（**只登记坐标**，红线）；`onServerStopped` 加
`ExplosionLedger.clearAllRuntimeState`（修掉旧静态 Map 漏清的内存泄漏）。

**测试**：`ExplosionParamsTest`（4 项）+ `ExplosionLedgerTest`（6 项，用 `ChunkApplier` seam
注入记录器，完全不需要真世界）。全量 **329 passed / 0 failed**（原 319）。

**测试当场抓到 2 个 bug**：① `carryOver` 写成 `addAll` 回原集合 ⇒ 队列永不收敛
（已改 `put` 替换）；② 测试里把"半径"当成"TNT 数"（radius 64 ≠ 64 TNT）。

**文档**：`living-tnt-tech.md` §1.2 数据流图 + §1.3 类表（顺带修掉 `ExplosionComponent`
的**过时路径**：写的是 `domain/tnt/`，实际在 `components/`）+ §4.3 全节重写；
infrastructure §3.2.2 标记方案 B 已实施；decisions `D-tnt-01`（新增 `tnt` 领域短码）；
file-map 源文件树 + 测试树；AGENTS 进展 + 基线 329。`doc_check.py` 6 项全过 0 警告。

## 追问：多场爆炸叠加会出什么问题（同日，推演 + 加固）

用户问「位图还没炸完，又引爆多个不同等级的爆炸会怎样」。逐条推演，**发现两个真 bug**：

**没问题 / 属取舍的**：
- 重叠区块落在多条条目内 → 对每条各处理一次；破坏**幂等**（第二次看到空气）⇒ 不重复掉落 ✓
- 不同等级/网格（SUPER 31×31 + NORMAL 3×3）→ `bitIndexOf` 各自独立 ✓
- 掉落物归属**取决于处理顺序**（先炸先掉、后炸看到空气）⇒ 不确定但不可观测（原版多 TNT 同理）
- 预算是 flush 级（非每条目）⇒ 同时 N 场时每场变慢（公平性，非 bug）

**🔴 bug 1：队列整体替换会丢新登记**（已修）
原 flush 结束时 `put(carryOver)` / `remove` **整体替换**队列 —— 若同 tick 期间又引爆一场
（`schedule` → `scheduleCheck`），新登记的区块被一起丢掉；而它们**已加载**、不会再触发
`ChunkEvent.Load` ⇒ **永远不炸**。
修法：遍历**快照** + 只 `remove` 本 tick 处理过的区块，队列永不整体替换。
守卫 `newRegistrationsDuringFlushSurvive`（applier 里模拟"处理中又引爆一场"）。

**🔴 bug 2：账本满时静默丢整场爆炸**（已修）
原 `schedule` 超上限直接 `return false`，调用方忽略返回值 ⇒ 声光与实体伤害已生效、
**方块一个没坏** ⇒ 玩家以为模组坏了。
修法：`ignite` 检查返回值，失败则降级 `applyToLoadedChunks`（只炸已加载，只用 `getChunkNow`
不强制加载）+ WARN。守卫 `schedule_rejectsWhenFull`。

**已知遗留（有意不处理）**：① 条目可能长期驻留（未加载区块玩家再也不去 ⇒ 永不完成）；
**不设过期** —— 过期=放弃，与用户否决的"跳过"同性质。② 同位置重复引爆不去重
（合并会改变 NORMAL 的掉落语义）。

测试 332 passed / 0 failed（+3）；§3.2.2 新增「多场爆炸叠加」小节（含推演表 + 两个真问题 +
已知遗留）；doc_check 6 项全过。

## 复审 + 「更优雅形态」评估（同日）

**复审结论：无新 bug**（两个真 bug 上一轮已修）。改了两处 + 补一条不变量注释：
- `applyToChunk` `public` → **包级**（它是"破坏唯一入口"，只应由账本经 `ChunkApplier` 调用；
  公开就能绕过账本直接破坏，封装形同虚设）
- `deleteChunkContent(level, chunk)` 收 chunk（原来又 `getChunkNow` 查一遍，调用方已有）
- `pendingChecks` 补**有界性不变量**注释：队列规模 ≈ 已加载区块数，**因为每 pass 丢弃未加载的**；
  "丢弃不是偷懒，是队列有界的前提"（若改成保留重试 ⇒ 无界膨胀 ⇒ 冻结）

**「更优雅」评估（写进 §3.2.2「复审」小节）**：
- **骨架不用推倒** —— 逐个排除替代：实体做账本（**可达性缺陷**：玩家从边缘进入时中心实体没加载）、
  异步预加载（真生成几百区块永久留档）、跳过（已否决）⇒ 账本是唯一合理选择。
- ⭐ **最值得的更优雅形态：把"位图 + 独立队列 + remaining"三状态合并成"每条目一个待办集合"**
  （`LinkedHashSet<ChunkPos>`）→ 消除 3 个互逆索引方法、网格/圆双判据、双重真相（load 不必重算）、
  flush 从 O(待检查×条目) 降到 O(总待办)；代价是存档 128B→7.7KB/条（`MAX_ENTRIES` 降到 32
  压回 250KB）。**建议暂不重构**（13 项守卫测的是位图语义，收益在"代码更少"非"行为更对"）；
  触发条件：加第四种爆炸模式时一起做。
- 三种模式 → 策略接口：收益只在扩展性 ⇒ 同上批。
- `pendingChecks` 与 `pendingRescans` 同构但**不抽公共抽象**（语义不同：无状态重扫 vs 有状态检查）。
- **已排除**：给 `onChunkLoaded` 加 `activeDims` 内存集合 —— `get(level)` 是"首次区块加载惰性
  读盘"的唯一入口，加了就得在启动时预热，而省下的只是 2 次 map 查找（~50ns）⇒ 不值。
- **一处归类问题（建议修，未动）**：`ExplosionLedger` 有状态却放在 `components/`，
  而该目录被描述为「**无状态**工具组件」；且 TNT 技术文档原本写 `domain/tnt/ExplosionComponent.java`
  ⇒ 最初意图是 `domain/tnt/`。建议三文件一起挪，或改目录描述。

**踩坑**：`./gradlew test --tests "*Explosion*"` 会**覆盖** `build/test-results/test/` ⇒
紧接着跑 doc_check 报「声明 332 / 实测 13」的**假失败**。已把这条写进 `livingitem-doc-system` 技能。

## 归类修复：爆炸三件套迁入 `domain/tnt/`（同日，已完成）

用户拍板修归类问题。已用 `git mv`（保留历史）迁移 5 个文件：
- `components/{ExplosionComponent,ExplosionParams,ExplosionLedger}.java` → **`domain/tnt/`**
- `test/.../components/{ExplosionParamsTest,ExplosionLedgerTest}.java` → **`test/.../domain/tnt/`**
（测试类必须同包才能访问包级 seam：`schedule` / `flush` / `MAX_CHUNKS_PER_TICK` / `remainingOf`）

改动：5 处 `package` 声明 + 4 处 import（`LivingTntFunction` 同包 ⇒ 直接删掉那行 import）。
`components/` 现在只剩 `ItemFilterComponent`，与「无状态工具组件」的定位一致 ✓

文档同步：file-map 源文件树（三条移到 `domain/tnt/`，标注迁入）+ 测试树（新增
`living/domain/tnt/` 段）+ AGENTS 子系统索引**顺带修掉一处过时描述**（活TNT 写的是
"双模式（普通/大当量）"，实际早已是三模式）+ tnt-tech §1.3 路径 + infrastructure §3.2.2
的归类问题段落标记 ✅ 已修。全库 grep 确认无旧路径残留。

**全量 332 passed / 0 failed**；doc_check 6 项全过 0 警告。
