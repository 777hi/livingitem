# 2026-09-16 工作日志

## 新增：放置活耕地回世界 → 自动种下自带作物

**需求（用户原话）**：「放置有种子的活耕地，会自动种下该种子。」澄清后 = 把一块**已种植**的
活耕地物品**放置到世界中**，放置出的耕地上直接长出那株作物。

### 设计演进（用户三轮定调，最终形态比初版简单得多）

1. **初版（已否决）**：`CropClassifier.getBlockFromSeed` + `stateForAge` + `level.setBlock`
   —— 硬塞方块。
2. **用户指出致命问题**：这会**绕过模组自己的种植校验**（如「水稻只能在水下种」）。
   → 改为**模拟玩家右键**：构造 `UseOnContext` 调 `ItemStack.useOn`。
3. **用户要求做减法**：「简单、鲁棒、兼容优先，只做最核心的逻辑」「不管成熟度」。
   → 砍掉多格作物上部件/柱段、`canSurvive`、`displayStateFor`、主格被占的种子掉落兜底、
   纯函数拆分；`age` 整块消失（原版种下天然 age 0）。
4. **用户定性为「软逻辑」**：「能种上就好，种不上不出 bug 就好」
   → 整段兜 try/catch + WARN 日志；失败静默不补偿。

**连锁收益**：用 `useOn` 后连 `CropClassifier`、`canBeReplaced` 检查、`is(Blocks.FARMLAND)`
检查**都不需要了**（种子自己的 `canSurvive` 校验下方是耕地）—— 方案同时变简单和变兼容。

### 落地（2 新文件 + 1 行配置）

- `domain/farmland/LivingFarmlandPlacement`（约 35 行）：`isPlantable` + `onPlaced`
- `mixin/BlockItemMixin`：`@Mixin(BlockItem.class)`，注入 `place` 的 `consume` **之前**
- `living_item.mixins.json` 加 `"BlockItemMixin"`

### 三个坑（都实际踩到/拦到，已写进 farmland §11.16）

1. **不能注入 `@At("RETURN")`**：`place` 末尾才 `consume(1)`，而空栈的 `getComponents()`
   返回 `DataComponentMap.EMPTY` ⇒ **单块放置静默失效、堆叠放置却正常**。
   → 锚在 `consume` 之前（已核对 `BlockItem.java` 全文仅 1 处 `consume`，无需 `ordinal`）。
2. **`Level.isClientSide` 是 `public final` 字段**，不是方法（方法只是读它）。Mockito
   只 mock 方法 ⇒ 我最初写字段式导致**单测里客户端守卫永远不触发**（测试红但代码看着对）。
   → 改用方法式 `level.isClientSide()`（语义等价，项目里 `EnderRouteManager` 已是此风格）。
3. **别自己 `setBlock`**（见设计演进 2）。

### 测试（`LivingFarmlandPlacementTest`，5 项）

`isPlantable` 真值表 / 落点在上方且一律 age 0（捕获 `setBlock`）/ 客户端不生效 /
**异常不冒泡**（软逻辑红线）/ **端到端 Mixin 接线**。

**关键发现：单元测试 JVM 里 Mixin 是被应用的**（证据：栈里出现
`BlockItem.handler$zzk000$create$fixDeployerPlacement` —— Create 的 mixin）。
⇒ 可以写**端到端用例证明 Mixin 接线**，不必等游戏启动：
调用 `((BlockItem) Items.FARMLAND).place(context)` 后断言作物被种下。
这条用例同时钉住坑 1（若注在 RETURN，空栈读不到组件 → 用例失败）。

**端到端用例的 mock 要点**（踩过才通）：
- `canPlace()` 要求落点**可替换** ⇒ 落点在放置前必须是空气、放置后才是耕地 →
  用 `thenAnswer` + `AtomicBoolean` 模拟状态迁移，固定 stub 过不了。
- `CropBlock.canSurvive` 还要查光照（`getRawBrightness ≥ 8`）→ 必须 stub。
- 还需 stub `enabledFeatures` / `isUnobstructed` / `setBlock`→true。

**全量：293 passed / 0 failed / 0 errors / 0 skipped**（288 + 5）。

### 文档同步

`living-farmland-tech.md` v1.10（§3.5 新节 / §10.1 文件清单 / §10.2 注册点 / §10.3 测试表 /
§11.16 三坑 / §12.1 实测清单）；`AGENTS.md`（文件树 farmland+mixin 段、已完成功能、进展条目、
用例总数 288→293）；`living-item-infrastructure.md` §9.7 补「放置链路不经
`SlotAccessor` / `SlotInteractions`」边界说明。

### 附带发现

**Create 也 Mixin 了 `BlockItem`**（`fixDeployerPlacement`），与本模组共存无冲突。

---

## 修复：活耕地种子图标在 HUD 快捷栏不渲染

**用户报告**：「活耕地的种子图标渲染在玩家快捷栏里，当玩家不打开背包的时候不渲染」。

**根因**：种子图标画在 `client/mixin/AbstractContainerScreenMixin.java` 的
`living_item$renderFarmlandCrops`（`@Inject(render, at=TAIL)`），这段代码**硬依赖
`leftPos`/`topPos`**——只有 `AbstractContainerScreen` 才有。而 **HUD 快捷栏**走
`Gui.renderHotbar` → `Gui.renderSlot` → `GuiGraphics.renderItemDecorations`
→ `ItemDecoratorHandler`，**从不经过 `AbstractContainerScreen`** ⇒ 永远不画。

**修复**：迁到 `IItemDecorator`（`client/render/LivingFarmlandSeedDecorator`，自抬 z=200）。
支点（已核源码）：`GuiGraphics.renderItemDecorations` 的**最后一行**就是
`ItemDecoratorHandler.of(stack).render(...)`，且容器 GUI 的 `renderSlotContents:307`
也调它 ⇒ **一份装饰器代码覆盖快捷栏 / 容器 GUI / 创造物品栏 / 副手槽，只画一次**。
容器 Mixin 里那段重复 blit 已删（`-24/+11`）；**生长槽大图保留**——它需要「同容器正上方
一格槽位」的邻居关系（`living_item$findSlotAbove`），装饰器只拿得到 `xOffset/yOffset`。

**三处易错契约**（都写进了 icon-system.md「种子图标改走装饰器路径」）：

1. 装饰器坐标 = **「在当前 pose 内 `translate(xOffset, yOffset, z)`」**。容器传的是
   `slot.x/slot.y`（**未加** `leftPos`——`renderSlot` 已 `translate(leftPos, topPos, 0)`）；
   快捷栏传绝对坐标（pose 为 identity）。**装饰器绝不能自己再加 `leftPos`**。
2. z 基准：容器 `renderSlot` 有 `translate(0,0,100)` ⇒ z=200 → **净 300**
   （正是旧注释里「z=300 才可见」的那个值）；快捷栏 pose 为 identity ⇒ 净 200。
3. `ItemDecoratorHandler` 每次调用前 `resetRenderState()`（**开**深度测试 + 开 blend），
   返回后 `restoreGlState` ⇒ **装饰器里不要写 `RenderSystem`**，旧 TAIL 版的
   `disableDepthTest()` 无法照搬。

**测试**：新增 `client/render/LivingFarmlandSeedDecoratorTest`（4 项，把 `shouldRenderSeed`
抽成包级静态供单测——装饰器按 `Item` 注册，会对**所有** FARMLAND 调用，守卫必须完整）。
全量 **297 用例全绿**（293 + 4）。客户端包下的单测在 FML 测试环境可正常编译运行。

**文档**：`icon-system.md`（三层架构 Layer 3 示例 + §5 表格**补上缺失的活耕地行** +
新增「种子图标改走装饰器路径」小节 + 关键文件表）；`living-farmland-tech.md` v1.11
（§1.2 决策表 / §8.2 / §10.1 / §10.2 / §10.3 / §12.3）；`AGENTS.md`（文件树 client/render 段 +
特性行 + 进展 + 用例总数 293→297）；`CropTextureResolver` javadoc。

**发现（未动）**：`AbstractContainerScreenMixin` 里 `import MapId` 是**既有**未使用 import
（非本次引入）；`icon-system.md` §5 表格还缺「活红石块」等行（既有遗漏，本次只补了活耕地）。

**未采纳**：架构代理建议改写 `icon-system.md` §「TAIL 阶段的深度陷阱」的根因叙述
（改说是物品模型遮挡而非世界深度竞争）——那是有理有据的历史复盘，本次只是把图标**换到
另一个渲染窗口**（该文档早已描述过这个窗口），不该用未证实的假设去覆盖它。

---

## 收尾：MEMORY.md 整理（超注入上限被截断）+ 发现一处文档缺口

**背景**：`MEMORY.md` 已涨到 **10528 字符**，超出注入上限 ⇒ 注入时被截断（等于尾部章节
在会话开始时失效）。

**整理做法**（**不删知识，只压缩措辞 + 把有别的归宿的细节换成指针**）：
- 先确认每个大块的「唯一家」在哪，再决定动不动：
  - **渲染像素规约**（symRound / `_sim_tooltip.py` / 环厚奇数…）→ `docs/` 里**零命中**，
    memory 是唯一记录处 ⇒ **完整保留**，并在节首标注「别删」。
  - 电力不变量 → 有 `power-invariants.md`；装饰器三契约 → 已进 `icon-system.md`；
    物品↔世界 / 跨容器传输 → 各有用户级技能 ⇒ 压成「关键红线 + 指针」。
- 结果：**10528 → 8535 字符（-19%）**，10 个章节全保留，抽查 38 项硬不变量
  （坐标常量、类名、阈值、grep 自检命令）**零丢失**。
- 顺手修掉两处陈旧：`已知遗留失败` 已并入「全量基线」节；基线数字更新为 297。

**⚠️ 发现的文档缺口（未擅自补，已报告用户）**：`docs/system-design/power-invariants.md`
（486 行）里 **完全没有** long 溢出红线——`溢出` / `1e23` / `Long.MAX` /
`MAX_LEFTOVER_PASSES` / `Integer.MAX_VALUE` / `转 double` **零命中**。
也就是说「曾让服务端冻结、且会凭空造电」的那条不变量**只活在会被截断的 memory 里**。
该文档体例是「语义不变量（I-A1…I-F3 编号）+ 四层测试映射」，补算术安全不变量需要
设计编号与测试层归属 —— **属设计决策，留给用户定**。

**另清理**：删掉调试时解压 MC 源码用的 `build/tmp-src/`（32 MB，我创建的临时目录）。

---

## 文档系统诊断（用户问「有什么优化建议」，2026-09-16）

**盘点结果**：`docs/` **50 篇 / 约 1.04 MB**；`AGENTS.md` **52,961 字符 / 1060 行**。

**AGENTS.md 构成（关键诊断）**：
| 章节 | 字符 | 占比 |
|---|---|---|
| 开发进展 | 24,801 | **47%**（append-only 日志，只会膨胀） |
| 核心文件索引 | 16,739 | **32%**（可从代码反推） |
| 已完成功能 | 3,956 | 7% |
| 子系统索引 / 文档导航 | 2,531 / 1,973 | 5% / 4% |
| 架构概览 / 项目概述 | 1,716 / 974 | 3% / 2% |

⇒ **79% 是「日志 + 文件清单」**，只有 21% 是稳定项目知识。用户「新对话加载 AGENTS.md 就懂项目」
的目标因此**结构性受损**，且会持续劣化。

**其它发现**：`docs/7.md` 空文件（垃圾）；`docs/problem.md` 77KB（首行是 mixin 报错堆栈，用途不明）；
`docs/置顶评论.md` 712 字符（版本信息，应并入入口）；`docs/archive/` 已有 10 篇（好实践）。

**给出的建议**（分 P0/P1/P2，已答复用户）：入口瘦身+分层 / glossary 术语表 / docs/README 维护规约 /
决策 ADR 化 + supersedes 链 / 文档断言校验脚本 / 清理垃圾。
**核心论点**：文档只写「代码里推不出来的」；文档要写成**约束**而非描述；文档缺**互补链纠错**
（今天一次终审就修了 7 处漂移，即证据）。

### 追加：归档流水线诊断（用户澄清「开发进展定期迁入 docs/archive/changelog.md」）

- 机制已有且体例统一：`changelog.md` 头部写明「从 AGENTS.md 迁出的历史更新记录」；
  AGENTS.md **:1009 已有指针**「2026-08-30 及更早的更新记录已迁至 changelog.md」✓
- **⚠️ 真实断档**：`2026-09-01 ~ 09-04` 的工作**在 changelog 与 AGENTS.md 里都是 0 命中**，
  只活在 `.workbuddy-ai/memory/2026-09-0{1..4}.md`（合计约 38KB）。而 memory 日志按策略
  **30 天后蒸馏并删除** ⇒ **约 2026-10-01 就会永久丢失**。丢失内容含：
  红电「网络级共振」设计定稿+实现、示波器相位波形修复、**信号层边模型重构（共享边→每槽自有出边 v15）**、
  比较器/中继器输出只能传一格的 bug、P1-4（7 张静态 Map 合并）/P1-5/6（processContext 瘦身）重构、
  **游戏崩溃修复（living_item_sync 包方向注册错误）**、全包方向审计。
- **条目体量**：开发进展 54 条 / 24,701 字符，**中位 452 字、最长 1148 字** ⇒ 是「正文」而非「索引」。
  后果：两次归档之间入口必然膨胀；归档后 changelog 同样膨胀（7 周 24.6KB ⇒ 一年约 180KB），
  **归档只是推迟问题**；且关键约束会被埋在流水账里（与今天发现的 `power-invariants.md` 缺溢出红线同型）。
- 建议：条目改「一行结论 + 指针」；归档加**连续性校验**（日期无空档 + 条数守恒 + 归档后只在入口留指针）；
  指针挪到节首；changelog 按月份拆 + 顶层做索引；可选做成定期 automation。

### 追加：按建议动手（补断档 + 指针前置）✅ 已完成

**1. 补断档（数据抢救）** —— 把 `2026-09-01 ~ 09-04` 从日工作日志整理进 `docs/archive/changelog.md`
（按该文件既有体例：`## 日期` + `✅/🐛 **标题** —— 详情` + `📄 技术文档`）。
changelog **24,612 → 34,365 字符**（+9.8KB 抢救回来的历史）。补录内容含：
- **09-01**：活末影箱「玩家绑定频道」架构分析（**用户关键澄清：专属频道是命名空间隔离、不是权限隔离
  ⇒ 直接砍掉整条同步层改造**）+ 读码确认的 4 个坑（filterData 被丢成 null / 路由永久残留 / 客户端缓存零调用者 / totalRoutes 污染）
- **09-02**：红电「网络级共振」定稿+实现（声部=锈蚀级、`R=1+(N−1)s`、满共振 ×16、
  **只读共振前基础值绝不回代**、`EMA_EPSILON` 归零截断）+ tooltip 四分区+示波器面板
  （**NeoForge `getTooltipImage()` 插索引 1、置底要走 `RenderTooltipEvent.GatherComponents`**）
  + 示波器波形修复 + 比较器输出只能传一格的 bug + **信号层边模型重构 v15（共享边→每槽自有出边）**
  + 稳态跳过优化（`recordSteadyState` 必须重采样**重算后**的 rev）+ 按网络遍历回归测试 + 末影箱 tooltip 增强 + 铜灯 lit 纹理修复
- **09-03**：比较器链 Bug A（改**迭代到稳定**）+ 无外接信号 Bug B（原版写入绕开 bump ⇒ 内容签名兜底）
  + P1-4（7 张 Map 合并）/P1-5/6（try-finally 生命周期）+ **屎山复查实为文档过时**
  （⚠️ 别照旧文档 §7.2 加回 Container 优先写入，会重新引入物品复制 bug）+ 巡检 + BOM 结论 + 整洁化
- **09-04**：**游戏崩溃修复**（`living_item_sync` 注册成 `playToServer`，应为 `playToClient`）
  + 全包方向审计（10 包，仅 1 错）+ 测试基线修正（146 是方法数，实际 169 用例）

**2. 指针前置 + 归档规则** —— AGENTS.md 原来那行指针在**第 1009 行**（翻完 24.8KB 才看见），
已移到「开发进展」**节首**（现 517 行）并补写归档规则 + **断档警示**；删掉原 footer 重复指针。

**3. 连续性校验（原缺失的那一步）** —— 脚本校验：changelog 最新 `2026-09-04` → AGENTS 最早
`2026-09-05`，**间隔 1 天、无空档 ✅**。注意区分：changelog **内部**的日期跳跃是「无工作日」而非漏记，
所以规则里把校验限定在**跨文件**那一处。

**规模**：AGENTS.md 52,961 → 54,279（+1.3KB，来自节首那段规则/警示）；changelog 24,612 → 34,365。
⚠️ **P0-1（把「开发进展」整体搬进 archive、AGENTS.md 压到 15KB）本轮未做** —— 用户本次只点了
「补断档 + 指针」，那是下一步。

### 追加：P0-1 + P0-2 落地（AGENTS.md 瘦身 -64%）✅ 已完成

**先定体例**（写进 AGENTS.md 开发进展节首）：一条 = **一行结论 + 指针**，细节进
`docs/tech/*` / `docs/system-design/*`；**只对新条目生效，不回改历史**。
归档规则修正：原写「约两周」粒度太粗（13 个批次全在两周内 ⇒ 永远不会触发），改为
**只保留最近 3 个更新批次**。

**P0-1 搬历史**：把 13 个批次里较早的 10 个（09-05 ~ 09-14 第二批）迁入 `changelog.md` 顶部，
转成 `## 日期` 体例（同日期多批次合并，如 09-07 的 4 个批次合成一段）。
AGENTS.md 保留 09-16 / 09-15 / 09-14 三个批次。

**P0-2 详单降级**：`核心文件索引`（240 行 / 13,999 字符的逐文件详单）→ 搬进新文件
`docs/reference/file-map.md`（**完整保留** + 顶部标注「快照、会漂移，准确清单用
`find src/main -name "*.java"`」），入口换成 **`## 模块地图`**（44 行 / 2KB，目录级 + 根级文件）。
**搬走而不是删掉** —— 零信息损失。

**规模总账**：AGENTS.md **54,045 → 19,555 字符（-64%）**、1060 → 425 行；
changelog 24,612 → 53,044（历史+详单的新家）；file-map.md 新建 14,184。

**校验（全部通过）**：
- 守恒：迁出的每段正文都能在新 changelog 里**原样找到** ✅
- 连续性：changelog 07-11→09-14、AGENTS 09-14→09-16，**跨文件间隔 0 天** ✅
- 结构：7 章节完整、旧标题 `核心文件索引` 零残留 ✅

**踩坑**：在 `bash -c "python -c '...'"` 里写含**反引号**的字符串，Bash 会把 `` ` `` 当**命令替换**
执行掉（把 `` `find ...` `` 替换成了实际文件路径，脚本直接语法错误）。**改成写脚本文件再跑。**

**沉淀**：新建用户级技能 `livingitem-doc-system`（分层职责与体量约束 / 归档 6 步含双校验 /
降级而非删除 / 脚本踩坑 / 新条目体例 / 血泪教训）。

**备份**（`build/doc-move-backup/`，gitignore 内，`gradle clean` 会清）：`AGENTS.md.bak`（P0-1 前）、
`AGENTS.md.pre-map`（P0-2 前）、`changelog.md.bak`（补录前）。

### 追加：⚠️ 用户纠正 —— 技能只对当前工具生效，项目级知识必须写在仓库里

**用户原话**：「我开发模组使用多个 ai 软件，写进技能只对当前软件生效，而非项目生效喵。」

⇒ 这**推翻了我上一轮的判断**（我曾说「规约已进技能，`docs/README.md` 价值下降」——结论正好反了）。
多工具协作下，`~/.workbuddy-ai/skills/` 里的规约换个 AI 工具就完全看不到，而且多份规约会各自漂移。

**已做的修正**：
1. **新建项目级规约 [`docs/README.md`](../../docs/README.md)**（事实来源）：分层与职责 + 体量约束 +
   新增内容该放哪 + **归档 6 步（含守恒 + 连续性双校验）** + 降级而非删除 + 新条目体例 +
   改完必查清单 + 脚本踩坑 + **已知缺口表**（decisions 未建 / glossary 未建 /
   `power-invariants.md` 缺溢出红线 / 校验脚本未做）。
2. **技能 `livingitem-doc-system` 降级为适配层**：开篇声明「事实来源在 `docs/README.md`」，
   正文只留工具侧执行要点（脚本别用 `python -c` + 反引号、备份位置、正则、交付方式）。
3. **另外两个项目技能**（`mc-livingitem-transfer-guardrails` / `mc-livingitem-mixin-guardrails`）
   各加声明块：列出对应的项目文档路径 + **「冲突时以项目文档为准」**。
4. **用户级记忆** `~/.workbuddy-ai/MEMORY.md`（新建）：记录「一个项目用多个 AI 工具」这一工作方式
   与推论（项目级知识写仓库、技能只做适配层、技能引用项目规则必须声明来源）。
5. **项目记忆 MEMORY.md**「文档系统的定位」节加指针，指向 `docs/README.md` 为事实来源。

**顺带修的漂移**：AGENTS.md `## 文档导航` 那棵树**也漂了**——`红电系统.md`/`活潜影箱实现细节.md`
实际在 `reference/` 却列在根、`reference/` 少列 3 个、`archive/` 少列 4 个、缺 `container-monitor.md`
与 `power-refactoring-plan.md`、缺新增的 `README.md`/`file-map.md`。已按真实清单重写为**按层组织 +
每篇一句话路由**，并加「完整清单以 `find docs -name "*.md"` 为准」的防漂移声明。
**校验：导航提及的 43 个文件名全部真实存在 ✅**。

**规模**：AGENTS.md 19,555 → 20,356 字符 / 438 行（导航节 1,973 → 2,781，因为给每篇补了路由说明）
—— 已到「≤20KB」线，下次新增内容应触发归档。

### 追加：技能 ↔ 项目文档 对照审计（逐条核查，补真缺口）

**方法**：把两个项目技能里每条**可核查的断言**抽成关键词，`grep -rl` 扫 `docs/`；0 命中即真缺口。

**查出并补齐的缺口（原本只写在技能里）**：

| 缺口 | 补到哪 |
|---|---|
| 测试复核口径：`--rerun`（`FROM-CACHE` 是**假绿**）/ 只认 XML `tests=` 求和 / **别用 `grep -c "@Test"` 数方法数** | `docs/guides/unit-testing.md` 新增「全量测试与复核口径」 |
| Mixin 接线与 mock Level：**单测 JVM 里 Mixin 是被应用的** ⇒ 可端到端验证接线；`canPlace()` 要求落点可替换（`thenAnswer`+`AtomicBoolean`）；`getRawBrightness ≥ 8`；`isUnobstructed`；`player=null` 安全 | 同上，「Mixin 接线与 mock Level」 |
| 测试 seam 约定（包级可见即可驱动）+ 改传输层必加的用例（准入真值表 / 每个入口都拒 / 普通与活物品各测一遍 / 方向化） | 同上，「测试 seam 与改传输层必加的用例」 |
| 改动教训：**隐式前提**（重构提公共入口要核对原调用点前提）/ **性能回退要数分配** / 口径翻转后 grep 旧结论 | `docs/tech/living-hopper-tech.md` §6.2.1 新增「改动教训」块 |
| **Mixin 层约定**：`required: true` + `defaultRequire: 1` = **有意的 fail-fast**；新增 Mixin 必须登记进配置；`@At` 描述符用**父类型**；注入别人方法内必须 try/catch；handler 保持无状态 | `docs/system-design/living-item-infrastructure.md` 新增 **§9.8** |
| 改文档时 `grep` 扫旧结论（`既有边界\|仍被拦下\|待复测\|已知遗留\|暂不支持`） | `docs/README.md` §7 校验清单 |

**复查结果**：原先 0 命中的 8 个关键词（`getRawBrightness`/`thenAnswer`/`isUnobstructed`/`AtomicBoolean`/
`隐式前提`/`数分配`/`FROM-CACHE`/`既有边界`…）现在**全部 ≥1 篇命中** ✅。
（注：`--rerun` 那次误报「仍缺」是 grep 把 `--` 当选项了，用 `grep -e` 复查确认文档里有。）

**同时更新**：两个技能头部的事实来源指针，加上新增的 `unit-testing.md` 与 `infrastructure §9.8`。

---

## 追加：按建议实施 ① 补溢出红线 ② 校验脚本（含一次自我纠错）

### ① `power-invariants.md` 增补 G 组「算术安全」

按该文档既有体例（`I-x#` 编号 + 分组详述 + 历史 bug 映射）补 **I-G1~I-G4**：
- **I-G1 份额不溢出**（`a*b/c` 先转 double 再夹 `remaining`；附 grep 自检命令）
- **I-G2 分配总额守恒**（`Σ distributed + leftover == accept`；`MAX_LEFTOVER_PASSES` 别删）
- **I-G3 宁损勿造**（share 异常宁可少充；`distributeToBulbs` 的 share 过大 → 凭空造电）
- **I-G4 请求量上界**（外部 mod 会传 `Integer.MAX_VALUE`）

同步：总表 `31 条，六组` → `35 条，七组`；§2.G 新节；§3 历史 bug 表加 3 行（16 → 19 条）；
**并如实标注「G 组的数值边界目前没有属性测试，§4.1 jqwy 是它的归宿」**。

### ② 新建校验脚本 `tools/doc_check.py`（并立刻抓到真问题）

5 项检查：路径真实性 / 测试条数与测试树一致性 / 归档连续性 / 入口体量。
前 4 项失败退出码 1，第 5 项只警告。已写进 `docs/README.md` §7.1。

**⚠️ 脚本首次运行就抓出我自己的一个真实损失**：
P0-2 搬运「核心文件索引」时，我用脚本只提取了该章节的**第一个代码块**（主树），
于是同一章节里的**测试文件树（2,513 字符 / 45 行）+ 测试基线声明（`合计测试用例 297 个`）被静默删除**，
既没进 `file-map.md` 也没留在 AGENTS.md。**守恒校验当时只验了第一块，所以没发现。**
已从备份恢复：测试树 → `file-map.md`「测试文件树」；测试基线 → AGENTS.md 模块地图节尾（用回原措辞）。

**脚本抓出的真实断链（已全部修复）**：

| 问题 | 数量 | 修法 |
|---|---|---|
| `红电系统.md` 迁到 `reference/` 后链接没跟着改 | 8 处 | 补 `../` 前缀 |
| `living/core/` 包早已重组（实际在 `living/transfer/`） | 2 处 | 改路径 |
| `LivingChestContentsPacket` 类名已改（实际 `LivingChestAccessPacket`） | 1 处 | 改类名 |
| `AGENTS.md` 指向 `docs/framework-refactoring.md`（实际在 `archive/`） | 2 处 | 改路径 |
| `pinyin-search.md` 指向 4 份已删除报告 | 4 处 | 去掉链接 + 标注「文件已删除」 |

**脚本自身的两类误报也已修**（否则噪声会让人忽略它）：
① 归档层豁免（冻结历史引用已删文件是正常的）；② 相对链接同时尝试「文件目录」与「仓库根」两种解析
（项目里两种写法都有）；③ 含「未建/待建/已删除」的行豁免（文档讨论规划文件时不该判断链）。

**最终状态**：`✓ 全部通过（1 项警告）`，退出码 0。唯一警告是 AGENTS.md 20,631 字符 > 20,000 预算
—— **正确且可执行**：该归档一轮了。

### 教训（已写进 docs/README.md §7.2）

**守恒校验必须逐段判定，不能只验「第一块」** —— 章节里可能有多个代码块/多段内容，
只验第一个就会漏。这次漏掉的正是「同一章节里的第二块」。

---

## 追加：入口瘦身（真因是重复，不是历史）+ 术语表

### ① 预算超标：先查冗余，再动历史

`doc_check.py` 报 `AGENTS.md 20,631 > 20,000`。**但开发进展只剩 3 个批次，按规则已无可归档**
—— 说明瓶颈不在历史。查下来真因是**章节重复**：
**「子系统索引」（2,547 字符）与「文档导航」（2,781 字符）内容高度重合**（都是
「子系统/主题 → 一句话 → 文档」），而分层定义 `docs/README.md` §1 已经有了。

**处理**：把「文档导航」退化为「常用入口」短表（2,781 → 694），
「该读哪一篇」统一交给「子系统索引」。**AGENTS.md 20,631 → 18,548 字符，历史一个字没动。**
（顺带修掉子系统索引里的过时数字「31 条不变量」→「35 条（七组）」）

**教训已写进规约 §4**：**先查冗余，再动历史** —— 历史是最有价值的资产，冗余才是负债。

### ② 新建 `docs/glossary.md` 术语表（40 条）

按主题分组（核心概念 / 红电电力层 / 活耕地 / 传输槽位交互 / 渲染图标 / 工程约定），
每条 = **一句话定义 + 出处文档**；页首声明「口径以实现层文档为准，冲突时改这里」。
已接进入口（AGENTS.md 文档导航第一行）+ 规约 §1 分层表（新增「术语」层）。

**写之前逐个核实了定义**，避免把过时公式写进去：
- `解锁度 u = 调谐效率 × (n / 偏好周期)`（**「规律度」因子已被移除**，旧公式作废）
- `调谐效率 = (1 + cos θ)/2`；`合因子 = (eff_δ_sum)^(1+u)`（不含 P）；`eff_δ_sum = Σ√|Δ_i|`
- 声部 = 锈蚀级、`R = 1+(N−1)s`、平衡度 = GM/AM、mFE 定点制

**顺带澄清一处易误读**：`reference/红电系统.md` 顶部的 `> **vX.Y 修订**` 块是**逆序修订历史**
（记录**当时**的设计，含已废弃因子「规律度」与旧 `log₂` 公式）—— **不是当前口径**；
当前口径在 `living-power-tech.md` §3.1 与该文档 §690 处，**两份一致 ✓**。
已把「修订历史块 ≠ 当前口径」写进术语表（避免下一个人又被旧条目误导）。

### 状态

`python tools/doc_check.py` → **✓ 全部通过（0 项警告）**，AGENTS.md **18,640 字符**。

**剩余**：`docs/decisions/`（决策 ADR 化 + supersedes 链）—— 需先定体例，建议单独一轮。

---

## 追加：决策记录体例草案（`docs/decisions.md`，待用户确认）

用户同意「先给体例草案再动手」。已写 `docs/decisions.md`（116 行 / 4,099 字符），
**明确标注为草案，确认前不接进入口**（`docs/README.md` §8 已记「体例草案待确认」）。

**核心设计取舍：索引式，不是经典 ADR（每条一个文件）** —— 理由三条：
1. **决策正文已有归宿**：各技术文档的「定案记录 / §1.2 核心设计决策」小节本来就在写；
   再建一份是**重复**，而重复就会漂移（今天的教训）。
2. 真问题不是「决策没地方写」，而是**「翻转后没人知道旧结论还在哪」** ⇒ 需要的是
   **跨领域可见索引 + 强制留痕**，不是更大的文件树。
3. 单文件索引能一眼扫完、能 grep、能进 `doc_check.py`。

**体例要点**：编号 `D-<领域>-<序号>`（序号永不复用）；状态机 `生效 → 已被取代 / 已废弃`；
8 列索引表（ID/日期/领域/决策一句话/理由要点/状态/取代关系/正文指针）；
**翻转 4 步（新增行 → 旧行留痕不删 → grep 扫旧结论 → 更新正文）**；
已填 11 条样例（含 2 条**已被取代**的：`D-hopper-02` 活骨粉放行、`D-power-03` 旧 log₂ 公式）。

**诚实边界（写在草案里）**：本索引**不能**自动发现「javadoc 与代码相反」这类漂移
（今天那处就是）—— 那靠代码审查 + `doc_check.py`；索引只覆盖「文档/注释里的旧口径」。
并建议后续给 `doc_check.py` 加一条：**校验「取代关系」双向一致**。

**待用户确认 5 项**：文件位置（单文件 vs 目录）/ 正文归属 / 编号方式 / 回填范围 / 字段增减。

---

## 追加：决策索引定稿 + 用户给出文档第一性原理

**用户回应**：「按照你自己的建议和感觉来喵，**文档基本上都是给 ai 看的**喵」
⇒ ① 5 项全部按我的建议定；② 且这句话本身是一条**第一性原理**，已写进 `docs/README.md` §1 **铁律 0**：

> **文档的主要读者是 AI** —— 由此推出全部规则：写**约束**不写叙述、**只写代码里推不出来的**、
> 断言必须**可复算**、**索引与正文分离**、**翻转必须留痕**。判断一篇内容怎么写时，
> 先问「AI 读它会不会被误导」。

**决策索引定稿 `docs/decisions.md`**（去掉草案标记，**18 条**决策）：
- 体例：`D-<领域>-<序号>` / 状态机 / 8 列索引表 / 翻转 4 步（**旧行不删**）
- **含 2 条「已被取代」的留痕样例**：`D-hopper-02`（活骨粉放行 → 被 D-hopper-01 取代）、
  `D-power-03`（旧 `log₂` 公式 + 规律度 → 被 D-power-02 取代）
- 补了 `D-core-03`「文档主要读者是 AI」与 `D-doc-01~03`（入口 ≤20K / 归档只留 3 批次 /
  搬走不删掉且守恒要逐段判定）

**校验脚本加第 6 项检查**：`decisions.md` 的 **supersedes 链双向一致**
（A 说取代 B ⇒ B 必须说被 A 取代）—— 这是让「翻转留痕」不依赖人记性的唯一办法。
**结果：6 项全绿，18 条决策通过。**

**接入**：AGENTS.md 文档导航加「查口径是否已被取代 → decisions.md」；
`docs/README.md` §1 加「决策」层 + 铁律 0，§7.1 加检查 6，§8 缺口清空（改为「暂无」）。
AGENTS.md 现 18,718 字符（< 20,000 ✓）。

## 追加：新 AI 入口实测（功能解释任务）+ 路由微修

**模拟新 AI（唯一从 `AGENTS.md` 开始）**的结果：**9/10**。
- 路由顺序顺畅：`AGENTS.md` → 子系统索引「活耕地」→ `living-farmland-tech.md` §3.5/§10/§11.16 → 代码验证；
  能正确说出 `BlockItemMixin`、`consume` 之前注入、为什么不能 `setBlock`、软逻辑边界。
- 发现唯一入口缺口：子系统索引「活耕地」概述**没提「放置回世界」**，只有下钻到开发进展才看得到。
  已修为「GUI… + **放置回世界模拟右键种植** + …」，并把指针精确到 §3.5 / §8。
- 测试/校验指引可靠：当前 297 用例、`doc_check.py` 6 项均可复核。
- 代理第二个「模拟 bug 排查」因平台频率限制未能启动；不影响上面的只读实测结论。按同一入口人工复核，
  活漏斗 bug 应读 `living-hopper-tech.md` §6.2.1/§6.2.2 + `unit-testing.md` 全量复核口径，路径成立。

## 追加：终审后把「文档无归宿」的知识迁进仓库 + MEMORY 瘦身

**终审发现唯一黄色项**：`MEMORY.md` 又涨到 **10,520 字符**，超注入上限 ⇒ 尾部三节可能被截断。
按用户自己的原则（**项目级知识必须写在仓库，工具侧只做适配层**），正确解法不是再压缩一遍，
而是**把没归宿的内容迁进 `docs/`，然后让 MEMORY 降为「红线 + 指针」**。

**先查哪些真的没归宿**（grep 确认，避免瞎搬）：

| 内容 | 原状态 | 迁到 |
|---|---|---|
| 涂蜡铜灯纹理约定（`232,160,62`、60px 黄框） | **✗ 无归宿** | `icon-system.md`「纹理约定」 |
| 相位圆盘渲染像素规约（`symRound` / `_sim_tooltip`） | **✗ 无归宿** | `tooltip-system.md`「相位圆盘渲染像素对齐规约」 |
| 性能判读覆盖盲区（PerfMetrics） | 只有指针没有正文 | `living-item-infrastructure.md` §10.1 |
| 配方书 stackedContents / 能量接口 / 三条链路 | 已有归宿 | 保持，MEMORY 只留指针 |

**结果**：
- `MEMORY.md` **10,520 → 5,823 字符**（-45%，注入截断风险解除）
- 三篇文档各增一节（`icon-system` +445 / `tooltip-system` +1,454 / `infrastructure` +395）
- `doc_check.py` **6/6 全绿**；代码测试 **297 passed / 0 failed**

**结论**：终审的黄色项已清零。当前文档系统 = 55 篇 / 1,086,099 字符，入口 18,746 字符，
术语 43 条，决策 18 条，校验 6 项，技能 3 个均为适配层。

---

## 追加：把「知识必须有仓库归宿」写进规约（用户「好的」）

- `docs/README.md` §1 **新增铁律 5**：**每份知识都必须在仓库里有归宿** —— 发现某知识点
  只写在工具侧技能或 `MEMORY.md` ⇒ 迁进 `docs/`，否则对其它工具不可见且可能被记忆截断丢失。
  同时给分层表补了「工具侧（适配层）」一行。
- §1 分层表里有一行陈旧项（`决策 / docs/decisions/* / **规划中**`）与已建成的 `decisions.md`
  重复且过时 —— 一并删掉（**这是又一处「旧结论残留」**，符合预期：每次终审都能扫出这类）。
- §7.2 人工清单**真正补上两条**（此前那次编辑是空操作，实际没写进去）：
  ① **入口路由抽样验证**（新增能力后模拟新 AI 从 AGENTS.md 走一遍）
  ② **知识归宿检查**（`grep -rl "<关键词>" docs/` 无命中 = 无归宿 ⇒ 迁进仓库）
  两条都附了 2026-09-16 的真实案例，便于下一个执行者照做。

**最终**：铁律 6 条（0~5），`doc_check.py` 6/6 全绿，代码测试 297 passed / 0 failed。

---

## 追加：0 引用文档排查（用户问「参考层/指南层要不要合并」）

**用户直觉**：这两层"更像是给人看的，不是给 ai 看的"，想合并。
**我的判断**：不同意合并，但直觉指向了真问题 —— 我统计了每篇被引用次数，发现：

| 文档 | 被引用 | 真实性质 |
|---|---|---|
| `reference/红电系统.md` | **6 次** | 红电**总体设计**，放在「参考」里是**严重错位** |
| `guides/unit-testing.md` | 2 次 | "怎么验证"，**强 AI 向** |
| `guides/framework-evolution.md` | **0 次** | 已完成重构记录 → 该归档 |
| `reference/block-facing.md` | **0 次** | **项目核心逻辑**（见下） |
| `reference/pinyin-search.md` | **0 次** | 功能还活着（见下） |

**关键发现（推翻"这两层是给人看的"这个前提）**：
- `block-facing.md` §6-8 讲 `Pos2D` ↔ `Direction` 的 `worldToGrid`/`gridToWorld`，
  代码验证 `CrossContainerTransfer.java:169/258/291` **真的在用** ⇒ 是**活的核心逻辑**，
  只是 0 引用 = **孤儿文档**。
- 但它的 §7 文件清单 **3/6 已失效**：`DirectionModeComponent`、`HybridContainerResolver`、
  `ContainerCacheManager` 三个类**已不存在**（v8 重构），`SlotResolver` 也搬家了
  （`living/core/` → `living/transfer/`）。
- `pinyin-search.md` 功能**还活着**（`PinyinHelper.java` + `RecipeBookComponentMixin.java` 都在，
  代码有 pinyin 逻辑）⇒ 不能归档。

**结论**：两层的病是**孤儿病**（0 引用），不是"给谁看"。合并治不了孤儿病。
⇒ **真正的判断标准应该是「这篇有没有被路由接上」** —— 接上了就是活的，没接上就是死的，
跟在哪一层无关。

**已做 3 件事**：
1. 修 `block-facing.md` §7：删 3 个失效类、改对路径、加「以 find 为准」+ 反向指针
2. 接路由：`living-hopper-tech.md` §6.2.2 → `block-facing.md`；
   `guides/recipe-book-style.md` → `pinyin-search.md`（两篇均由 0 引用 → 1 引用）
3. 精简 `pinyin-search.md`：顶部加「AI 读这里 → §技术架构/§性能分析/§测试用例」，
   把 50 行玩家向使用指南压成 15 行表格（信息全保留）

**顺带发现并清理**：`guides/recipe-book-style.md` **开头 10 行是 AI 对话记录泄漏**
（"好的！让我帮你梳理…用户想要我梳理…"）—— 已删除，正文（6 个章节）未动。

**遗留（未做，需用户定）**：`红电系统.md` 被引用 6 次却放在 reference 层，
建议升级到设计层；`framework-evolution.md`（0 引用 + 已完成重构）建议归档。





---

## 追加：手术备份挪出 build/（用户「那就挪一挪吧」）

**原因**：备份原在 `build/doc-move-backup/`，而 `.gitignore` 忽略 `build/` 且 `gradle clean` 会清空它
—— 而**那批文档改动当时尚未提交**，`git checkout` 救不回来，所以这层备份必须留。

**新位置**：`.workbuddy-ai/backup/doc-surgery-2026-09-16/`（4 份快照 + `README.md`）。
- **不放 `docs/`**：会被 `doc_check.py` 的路径检查扫到（快照里含早已失效的旧路径）
- **不放 `build/`**：`gradle clean` 会清
- `.workbuddy-ai/` 未被 gitignore ⇒ 备份会出现在 `git status` 的 untracked 里（**属预期，已在 README 注明**）

**README 内容**：每份快照对应哪个手术阶段 / 怎么恢复 / **提交进 git 之后即可删**（git 才是长期安全网）。

**同步修正技能**：`livingitem-doc-system` 里原写「备份到 `build/`」—— 那正是被清掉的位置，
已改为 `.workbuddy-ai/backup/<日期>-<主题>/` 并说明两个「不要放」的理由。

**验证**：`doc_check.py` 仍 **6/6 全绿**；`git status` 只有 2 个 untracked（含该备份目录）。

---

## 追加：红电系统文档定位确认 + framework-evolution 归档

**用户澄清**：`红电系统.md` 起初是开发计划草稿，随着开发逐渐充实；它描述的是**红电系统级设计**，
不是单个活物品的功能实现，而且具体实现已有 `living-redstone-tech.md` / `living-power-tech.md` 两篇技术文档。

**最终分类原则**：按「文档回答的问题」分层，不按「是否起源于草稿」分层：
- **系统级目标 / 边界 / 总体模型 / 跨子系统设计原则** → `system-design/`
- **单个领域的代码链路 / 数据流 / 踩坑 / 测试** → `tech/`
- **历史提案 / 已完成重构报告 / 不再作为当前规范的材料** → `archive/`

**已执行**：`docs/reference/红电系统.md` → `docs/system-design/红电系统.md`（不改文件名，减少外部认知成本）。
在文首加「文档定位」声明：系统级设计源，不是草稿；具体实现去两篇 tech；文首 vX.Y 块是逆序历史，
当前口径以正文 + decisions + power-invariants 为准；系统级决策要同步 decisions。

**同步修复**：AGENTS common entry、glossary、decisions、living-power-tech、活潜影箱历史迁移链接；
docs/README 的断链示例更新为「曾迁 reference，后升级 system-design」。红电文档内部指向活潜影箱的相对链接全部修正。

**framework-evolution**：用户已确认归档，已从 `docs/guides/` 移至 `docs/archive/framework-evolution.md`。
它是已完成重构记录，0 引用，归档而非删除，历史仍可查。

**验证**：`python tools/doc_check.py` **6/6 全绿**，入口 18,750 字符；代码未改。














