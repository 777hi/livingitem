把 `.trae/skills/` 里的 3 个 skill 安装到 ZCode 工作区 skill 目录（不含 trae-terminal-workaround）。

## 安装内容

复制到 `<repo>/.zcode/skills/`（工作区级，因为这些 skill 引用的都是本项目相对路径 `libs/src/...`、`build/moddev/artifacts/`）：

| Skill | 用途 |
|-------|------|
| `mc1-21-1--` | 浏览 MC 1.21.1 + NeoForge 21.1.230 合并源码（`libs/src/neoforge-21.1.230-merged/`） |
| `neoforge-docs` | 查询 NeoForge 官方文档（`libs/src/Documentation-main/`） |
| `vanilla-jar` | 浏览/提取原版 jar（`build/moddev/artifacts/` 下的 4 个 jar） |

## 具体步骤

1. 创建 `G:\777hi\mc\mymods\livingitem-template-1.21.1\.zcode\skills\` 目录
2. 复制 3 个 skill 目录（各含 SKILL.md）过去，frontmatter（name/description）格式与 ZCode 兼容，无需改动
3. 小调整：`vanilla-jar/SKILL.md` 里的示例命令是 PowerShell 语法（`Select-String`），本环境 shell 是 Git Bash，把示例改成 bash 等价写法（`jar tf ... | grep` / `unzip -l ... | grep`），保证 skill 实际可用
4. 验证文件复制完整

## 说明

- 装完后新会话生效（当前会话的可用 skill 列表是启动时加载的），之后我在需要查原版源码/NeoForge 文档/jar 资源时会自动触发这三个 skill
- `.trae/skills/` 原目录保持不动，不删除