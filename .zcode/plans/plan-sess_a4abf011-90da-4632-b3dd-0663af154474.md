# 堆叠行为文档化（跨容器回归修复暂缓，待用户探讨）

## 改动（纯文档，一处）

living-farmland-tech.md §7.1「节奏」+「消耗」两小节合并改写为**堆叠行为表**（四行）：

| 维度 | 行为 |
|------|------|
| 施肥间隔 | 冷却 `max(1, 8 − N/8)`，N=漏斗堆叠数：1 个 8t/次、8 个 7t、56 个以上每 tick 一次（漏斗通用冷却公式，施肥 true 自然设冷却——与普通传输同节奏） |
| 单次剂量 | 固定 1 粉/次，**不随漏斗堆叠放大**（普通传输量是 min(N,64)/次；施肥一次生长 tick 只值一粉，按堆叠放大则 64 漏斗一次烧 64 粉） |
| 堆叠耕地联动 | **1 粉 = 整叠耕地同步 +1 age**（堆叠耕地共享一个 FARMLAND_PLANT 组件）+ 产出 ×耕地堆叠数 |
| 空转守卫 | 整叠冻结 → equals 零空转一滴不烧（对堆叠同样生效） |

表后补两段：堆叠性价比结论（最优 = 大堆耕地 + 堆叠漏斗：节奏快/一粉多头/冻结零浪费；1漏斗配1耕地是最差配比）+ 骨粉堆=续航、用完自动停。

事实来源（已读代码核实）：LivingHopperFunction.java:126 冷却公式 `Math.max(1, DEFAULT_COOLDOWN - stack.getCount()/8)`、:120 传输量 `stack.getCount()`；施肥分支固定 shrink(1)；堆叠耕地共享组件（FarmlandPlantComponent 不进 ignored 集合）。

## 不做（待探讨）
- 跨容器回归（无耕地邻居拦断普通货物推送）：**不动代码**，等用户探讨后再定方案
- AGENTS.md 不动（无代码变更，等跨容器定案后一起收编）

## 验证
纯文档改动，无编译/测试影响；全量 268 基线不变